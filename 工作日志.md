# 拾光素材库 — 项目工作日志（交接文档）

> 编写时间：2026-08-04
> 用途：供后续接手 Agent 快速了解项目现状、架构设计与开发注意事项

---

## 一、项目概述

**拾光素材库**（内部包名 `eagle-like`）是一个类 Eagle 的本地素材管理桌面应用，
用 Electron 构建，支持图片/视频/音频等多格式素材的导入、整理、检索与编辑。

- **当前版本**：0.1.0（已打包出 Windows 安装包，功能完整可用）
- **工作区根目录**：`e:\类eagle搭建`
- **产物位置**：`dist\拾光素材库-0.1.0-setup.exe`（安装包）、`dist\win-unpacked\ShiGuangMaterials.exe`（绿色版）

---

## 二、技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 桌面框架 | Electron 41 | 主进程 + preload + 渲染进程三进程架构 |
| 构建工具 | electron-vite 3 + Vite 6 | 开发热更新 / 生产打包 |
| 前端 | React 19 + TypeScript 5.9 + Tailwind CSS 4 | 渲染进程 UI |
| 状态管理 | zustand 5 | `src/renderer/src/stores/libraryStore.ts` |
| 数据库 | better-sqlite3 | 元数据存储在库目录的 `library.db` |
| 图像处理 | sharp | 缩略图、主色提取、dHash 指纹 |
| 视频封面 | ffmpeg-static | 导入视频时提取首帧 |
| 打包 | electron-builder | NSIS 安装包 |

---

## 三、目录结构

```
e:\类eagle搭建\
├── package.json              # scripts: dev / build / build:win / typecheck / postinstall
├── electron.vite.config.ts   # main/preload/renderer 三段构建配置
├── electron-builder.yml      # 打包配置（含 ffmpeg asarUnpack、图标、NSIS）
├── tsconfig.node.json        # 主进程/preload/shared 类型
├── tsconfig.web.json         # 渲染进程类型
├── .npmrc                    # 国内镜像配置（electron/sharp 二进制）
├── build/                    # 应用图标 icon.ico / icon.png（scripts/make-icons.cjs 生成）
├── scripts/
│   └── make-icons.cjs        # 图标生成脚本（琥珀金渐变 + “拾”字）
├── browser-extension/        # Chrome 剪藏扩展（MV3）
│   ├── manifest.json / background.js / popup.html / popup.js
│   └── icons/
├── src/
│   ├── shared/types.ts       # 主/渲染进程共享类型（Asset/Tag/Folder/SmartConditions 等）
│   ├── main/                 # 主进程
│   │   ├── index.ts          # 入口：窗口、asset: 协议、剪藏服务、监控、回收站清理
│   │   ├── db.ts             # SQLite 初始化与增量迁移（ensureColumns）
│   │   ├── library.ts        # 多库配置管理（libraries 列表 + current + watchDirs + importMode）
│   │   ├── repository.ts     # 全部数据 CRUD / 查询引擎 / 重复检测 / 回收站清理
│   │   ├── importer.ts       # 导入管线：复制入库、缩略图、主色、dHash、视频封面、metadata.json
│   │   ├── editor.ts         # 编辑器保存：覆盖原图 + 重生成缩略图/主色/尺寸
│   │   ├── clipServer.ts     # 浏览器剪藏 HTTP 服务（127.0.0.1:45678）
│   │   ├── watcher.ts        # 监控文件夹自动导入（fs.watch + 防抖）
│   │   ├── ipc.ts            # 所有 IPC 通道注册
│   │   └── ffmpeg-static.d.ts
│   ├── preload/index.ts      # contextBridge 暴露 window.api（唯一渲染层 API 面）
│   └── renderer/
│       ├── index.html
│       └── src/
│           ├── index.css     # 设计令牌 + btn-*/field-input 通用类 + 动效规范
│           ├── App.tsx       # 布局、快捷键、粘贴导入、剪藏通知
│           ├── stores/libraryStore.ts  # 全局状态（视图/筛选/选择/历史）
│           └── components/   # Sidebar/Toolbar/Gallery/Inspector/Preview/Editor
│                             # SmartFolderDialog/SettingsModal/DupeModal/ColorWheel/ConfirmDialog
└── dist/                     # 打包产物
```

---

## 四、核心架构设计

### 4.1 素材库存储结构（类 Eagle）
```
C:\Users\DL\Documents\EagleLike.library\   ← 默认库（路径存于 userData/config.json）
├── library.db                    # SQLite 元数据
└── assets/{id前2位}/{id}/
    ├── {id}.{ext}                # 原文件
    ├── thumbnail.jpg             # 512px 缩略图
    └── metadata.json             # Eagle 兼容元数据
```
- 多库支持：config.json 存 `libraries[]` + `current`，切换时 closeDb → ensureLibrary
- 渲染进程通过自定义 `asset://` 协议（`protocol.handle`）加载库内文件，避免 file:// 权限问题
  - `asset://{id}/file?t=t` 缩略图，`?t=o` 原图

### 4.2 数据库表
- `assets`：id/name/ext/rel_dir/size/width/height/colors(JSON)/hash(dHash)/star/comment/url/created_at/imported_at/deleted_at
- `tags` + `asset_tags`、`folders`（含 is_smart + conditions JSON）+ `asset_folders`
- 旧库兼容：`db.ts` 的 `ensureColumns` 做增量迁移（新增列用 ALTER TABLE）

### 4.3 IPC 通信
所有通道在 `src/main/ipc.ts` 注册，preload 中一一映射为 `window.api.*` 方法。
渲染进程**只能**通过 `window.api` 访问主进程能力（contextIsolation + sandbox:false）。

---

## 五、已完成功能清单

### 基础框架
- [x] 素材导入（拖拽窗口 / 导入按钮 / Ctrl+V 粘贴），复制入库 + 缩略图 + 主色提取 + 去重
- [x] 瀑布流图库 + 虚拟滚动（最短列布局，仅渲染视口 ±600px）
- [x] 标签 / 文件夹 / 收藏夹（星级）管理，拖拽素材进文件夹（可多选批量）
- [x] 搜索、格式筛选、星级筛选、排序（导入时间/名称/大小/评分）
- [x] 大图预览（左右切换、视频/音频播放）、右键菜单
- [x] 回收站（软删除/恢复/永久删除/清空）+ 启动时自动清理 30 天前内容 + Ctrl+Z 撤销删除

### 进阶功能
- [x] 智能文件夹：关键词/标签/格式/星级/主色/宽度范围/大小范围/导入时间 条件组合，数量实时统计
- [x] 颜色色环选择器（Canvas：角度=色相、半径=饱和度 + 明度滑块 + 容差）+ 颜色数量筛选
- [x] 重复素材检测：导入时计算 dHash（9x8 灰度差分），查重弹窗分组展示、默认勾选副本
- [x] 图片编辑器：画笔/矩形/箭头/文字批注 + 裁剪（比例锁定 1:1、4:3、16:9、3:4、9:16）+ 15 步撤销
- [x] 视频封面提取（ffmpeg-static 取首帧，读尺寸与主色）
- [x] 浏览器剪藏：本地 HTTP 服务（127.0.0.1:45678 /clip /status）+ Chrome MV3 扩展
      （整页批量 + 右键单图，CORS 失败自动回退应用端下载），记录来源 URL 可在检查器打开
- [x] 多库切换：侧边栏底部库切换器（新建/打开/切换/移除记录）
- [x] 设置页：导入模式（复制/移动）、监控文件夹自动导入（fs.watch）
- [x] 快捷键：Delete 删除、Ctrl+Z 撤销、Ctrl+A 全选、空格预览、方向键移动、1-5 评分、Esc 关闭
- [x] 右键「在资源管理器中显示」「复制图片到剪贴板」

### UI 工程化（依据 Web Interface Guidelines 重构）
- [x] 设计令牌体系（5 级灰阶 + 琥珀金品牌色，见 index.css :root）
- [x] 语义化（nav/aside/dialog/dl）、aria 标注、:focus-visible 焦点环、prefers-reduced-motion
- [x] 统一控件类：btn-primary / btn-ghost / btn-danger / btn-danger-solid / field-input
- [x] 破坏性操作确认弹窗（ConfirmDialog）、弹窗/Toast 入场动画、tabular-nums 数字对齐

### 自动更新（electron-updater，2026-08-05）
- [x] 品牌定名 **LUMEN**（productName / 窗口标题 / 界面字标 L U M E N 宽距排印 / 剪藏服务 / LUMEN Clip 扩展；
      内部包名仍为 eagle-like，安装目录与更新标识不变；安装包名 Lumen-X.X.X-setup.exe）
- [x] 图标：L + 终端光标（L_）几何标，蓝黑像素阶梯角底 + 故障切片 + 像素点（scripts/make-icons.cjs）
- [x] 更名「拾光档案馆」（已被 LUMEN 取代）
- [x] Shift 范围多选：store 记录 anchorId，selectRange 选中锚点到目标的连续区段（Windows 式）
- [x] 监控文件夹：fs.watch recursive 递归子目录；多目录本即支持（设置页可加多个）
- [x] 主进程 `updater.ts`：状态机（checking/available/downloading/downloaded/none/error）→ IPC 推送渲染进程
- [x] 启动 8s 静默检查；`autoDownload=false`，发现更新由用户确认才下载；`autoInstallOnAppQuit` 默认开（退出时自动装）
- [x] 渲染层：右下角更新卡片（版本号 + 立即下载/稍后）→ 下载进度条 → ConfirmDialog 重启安装
- [x] 设置页「关于」：当前版本号 + 手动「检查更新」（已最新/失败 toast 仅手动触发时提示，静默检查不打扰）
- [x] `electron-builder.yml`：`publish: github`（yuanzhoucanxiang/shiguang-materials，公开仓库，**仅放安装包，无源码无素材**）
  - 安装包文件名必须用 ASCII（`ShiguangMaterials-X.X.X-setup.exe`）：GitHub 附件名会剥掉非 ASCII 字符，中文名会导致 latest.yml 引用 404
  - 发版流程：版本号 +1 → `npm run build:win` → `gh release create vX.X.X ... setup.exe + blockmap + latest.yml`
  - 已验证 latest.yml 与安装包均可从 Release 直接下载 ✓
- [x] 端到端验证：本地静态服务挂 dist/，0.2.0 客户端 → 检出 0.2.1 → 全量下载 147MB → 安装 → 重启为 0.2.1 ✓
  - 注意：静态服务需支持 Accept-Ranges 才有差量更新（blockmap），否则回退全量下载（本机 nginx/OSS/GitHub 均支持）
  - 开发模式（!isPackaged）update:check 返回 `{state:'dev'}`，不触发真实检查

### 功能补全（对标 Eagle，2026-08-04）
- [x] 剪藏扩展增强（LUMEN Clip）：区域截图（页面内拖选 + OffscreenCanvas 裁剪）、
      整页长截图（滚动分段截取 + 拼接，限速 600ms/段，上限 12000px）、批量按最小宽度与格式过滤
- [x] 导出：右键「导出到文件夹…」（保留原名，重名自动 (1) 后缀）/「打包为 ZIP…」
      （纯 JS store 模式 ZIP 写入器，含 metadata.json 清单，UTF-8 文件名）
- [x] 字体预览：fontkit 读取字形轮廓 → SVG → sharp 渲染样张（CJK 自动检测选样例文本、
      单行自适应缩字号、TTC 取首个字体），ttf/otf/ttc/woff/woff2 支持；格式筛选加「字体」组
- [x] 标签颜色：tags.color 字段启用，右键色板（10 色 + 清除）；侧栏颜色点 + 检查器彩色 chips
- [x] 标签分组：tag_groups 表 + tags.group_id 增量迁移；侧栏分组渲染（折叠/计数）；
      右键移动到分组/新建分组并移入/重命名/删除分组（标签保留）
- [x] 智能文件夹扩充：构图（横/竖/方，±5% 容差）、颜色数量、未标注条件
- [x] 右键菜单按高度钳制，不再溢出视口底部
- [x] 预览缩放：滚轮以光标为中心缩放（1x–8x）、放大后拖动平移、双击复位、百分比指示
- [x] 悬停播放：悬停面板内 GIF 自动播放原图、视频静音循环、音频自动播放
- [x] 布局切换：瀑布流/网格/列表三模式（工具栏右侧切换，列表含名称/星级/格式/尺寸/大小列）
- [x] 批量星级：多选批量评分（检查器批量面板）
- [x] 筛选补全：未标注（无标签）筛选、导入时间档（今天/7/30/90/365 天）
- [x] 悬停放大预览：卡片悬停 420ms 弹出大预览（缩略图 + 格式/尺寸/大小读数，pointer-events:none，滚动/点击即收）
- [x] 相似图片搜索：dHash 汉明距离引擎（hammingBytes），查重从精确匹配升级为 ≤6 并查集归组；
      右键「搜索相似图片」以图搜图（≤10，按相似度排序，顶部横幅可退出）
- [x] 多级文件夹树：缩进渲染 + 折叠箭头 + 右键「新建子文件夹」；删除文件夹时子级自动上移（re-parent）
- [x] PSD 缩略图：ag-psd 解码合成图（initializeCanvas 注入纯 JS ImageData 工厂，免 node-canvas），
      无合成图时优雅降级图标；预览原图失败自动回退缩略图
- [x] 批量标签：多选时检查器出现批量打标签面板（追加式，不覆盖既有标签）

### UI 美术：死亡搁浅终端风 × 故障艺术 × 像素艺术（2026-08-04）
- [x] 墨黑蓝调体系：蓝黑背景（#07090d 等 5 级）+ 发丝线框 + 淡蓝光晕、细扫描线、Bayer 4×4 有序抖动
- [x] 主色冰蓝 #4da9e9、星级银蓝 #7ec9f2（单色冷调体系）；等宽数据字体 .mono（Cascadia Mono/Consolas）
- [x] 故障艺术：品牌标题 RGB 通道撕裂（红/青双层切片周期短促爆发）、Toast 故障切片入场、
      ScrambleText 乱码解码组件（副标/Toast 逐位收敛）、卡片图像 hover 故障爆发 + 扫描线覆盖
- [x] 像素艺术：主按钮像素阶梯角（clip-path 两级阶梯）、选中角标小阶梯角、区块标题像素星点、
      PixelArt 点阵渲染组件（box-shadow 像素群）驱动的空态像素画（琥珀太阳 + 青山水）
- [x] 直角卡片 + 对角框线选中态（.ticks 蓝角标）、字标 + ARCHIVE // NNN 读数、SYNC 状态行
- [x] 截图走查工具：`.ui-shot/`（offscreen 渲染；mock 数据 + PNG 生成器，`npx electron .ui-shot/main.cjs`，
      `SHOT_EMPTY=1` 可拍空态）

---

## 六、常用命令

```powershell
npm run dev           # 开发模式（主进程改动自动重启，渲染进程 HMR）
npm run build         # 仅构建 out/
npm run build:win     # typecheck + 构建 + electron-builder 打包安装包
npx tsc --noEmit -p tsconfig.node.json --composite false   # 主进程类型检查
npx tsc --noEmit -p tsconfig.web.json  --composite false   # 渲染进程类型检查
node scripts\make-icons.cjs   # 重新生成图标
```

**启动正式版**：`dist\win-unpacked\ShiGuangMaterials.exe`

---

## 七、环境注意事项与踩坑记录（重要）

1. **Node 版本**：本机 Node v26.1.0（ABI 147），Electron 41 内置 Node ABI 为 145。
   - better-sqlite3 已用 `@electron/rebuild` 编译为 **Electron ABI**，
     **不能**在纯 Node 脚本里直接 `require('better-sqlite3')`（会报 NODE_MODULE_VERSION 不匹配）。
   - 需要用 Node 环境操作数据库时，用 `npx electron 脚本.cjs` 运行。
   - `package.json` 的 postinstall 会自动 `electron-rebuild -f -o better-sqlite3`。
2. **网络环境（国内）**：直接下载 GitHub 二进制会失败，已配置：
   - `.npmrc`：electron_mirror / electron_builder_binaries_mirror / sharp 镜像
   - ffmpeg-static 安装需：`$env:FFMPEG_BINARY_HOST="https://npmmirror.com/mirrors/ffmpeg-static"`
   - 打包时建议：`$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"`
3. **中文路径**：控制台输出中文路径/文件名常显示乱码（编码问题），不影响功能。
4. **ffmpeg 打包**：electron-builder.yml 已配 `asarUnpack: **/node_modules/ffmpeg-static/**`，
   运行时路径通过 `app.asar` → `app.asar.unpacked` 替换（见 importer.ts `ffmpegBin()`）。
5. **素材库位置**：`C:\Users\DL\Documents\EagleLike.library\`（多库配置在 `%APPDATA%/eagle-like/config.json`）。
   注意：库文件夹名仍是 EagleLike.library（历史兼容，未随改名变动）。
6. **剪藏服务端口**：45678（固定，仅 127.0.0.1）。开发版与正式版同时运行会端口冲突（后启动者报错但不影响其他功能）。
7. **dist 目录**：有一个旧的英文命名安装包 `eagle-like-0.1.0-setup.exe`（130MB），可手动删除。

---

## 八、已知问题 / 待优化点

1. **AI/HEIC 无缩略图**：sharp 不支持 AI 解码，HEIC 依赖 libvips 构建；PSD 已支持（需文件含合成图，即保存时勾选「最大兼容性」）。
2. **瀑布流高度钳制**：imgH 限制在 `[0.35w, 1.8w]`，极端比例图片显示会裁切（object-cover）。
3. **~~编辑器保存为覆盖原图~~**：已改为非破坏性编辑，编辑结果存 `{id}.edited.{ext}`，原图保留，可「恢复原图」（见里程碑 29）。
4. **无代码签名**（SmartScreen 会提示）；安装包约 150MB（主要是 ffmpeg）。
5. **~~图库虚拟滚动阈值~~**：查询层已优化（构图/颜色数量下推 SQL + 新增索引，见里程碑 30），±600px 虚拟滚动阈值仍为经验值，超万级素材可进一步优化。
6. **更新检查只在应用启动 +8s 做一次**：不重启应用不会发现新版本（可在设置手动「检查更新」）。
7. **开发环境注意**：TaskStop 杀 npm 父进程后 electron 子进程可能残留（僵尸），会占用调试端口/剪藏端口；
   排查时用 `tasklist //FI "IMAGENAME eq electron.exe"` 找到后 `taskkill //PID xxx //F //T` 连树清理。
8. **~~文件夹视图只显示直接归入素材~~**：已改为递归 CTE 联动子文件夹（见里程碑 30）；文件夹暂不支持拖拽改变层级。

> 历史遗留已解决（勿再当作待办）：
> - ✅ 监控文件夹已支持递归子目录（`watcher.ts` 用 `fs.watch({ recursive: true })`，见里程碑 16）。
> - ✅ 自动更新已实现（`updater.ts` + electron-updater + GitHub Releases，见里程碑 14/15）。
> - ✅ 库备份已实现：启动自动备份 `library.db.bak` + 设置页手动导出完整库 ZIP（见里程碑 27）。
> - ✅ 错误日志已实现：`logger.ts` 落盘 `userData/logs/main.log`，importer 等模块的静默 catch 已补日志（见里程碑 27）。
> - ✅ 导入并发+事务：两阶段拆分（prepareOne 并发 + commitBatch 事务），按 CPU 核心数并发（见里程碑 28）。
> - ✅ 编辑器非破坏性编辑：原图保留，编辑结果另存 `{id}.edited.{ext}`，可「恢复原图」（见里程碑 29）。
> - ✅ 文件夹视图联动子文件夹：递归 CTE 查询子树（见里程碑 30）。
> - ✅ EXIF 元数据：导入时读取，Inspector 展示相机/光圈/快门/ISO/焦距（见里程碑 30）。
> - ✅ 视频故事板：导入时生成 4 帧拼图，悬停预览优先显示（见里程碑 31）。
> - ✅ 查重可调阈值 + ext 分组预过滤（见里程碑 32）。
> - ✅ dist 历史安装包已清理，仅保留最新版本（2026-08-05）。

---

## 九、后续推进建议（按价值排序）

1. **相似图片检测升级**：dHash 汉明距离 ≤6 归组（repository.findDuplicates 内扩展）
2. **图片格式补全**：接入 magick-wasm 或 psd.js 处理 PSD/AI 预览
3. **编辑器非破坏性编辑**：保存编辑副本而非覆盖原图，或记录编辑历史
4. **多语言设置页**、快捷键自定义
5. **自动更新**：electron-updater + 静态文件服务
6. **代码签名**与安装包瘦身（当前 130MB，主要是 ffmpeg）

---

## 十、开发里程碑回顾

| 阶段 | 内容 |
|------|------|
| 1 | 脚手架 + SQLite/sharp/IPC + 导入/浏览/标签/文件夹/颜色检索/回收站 |
| 2 | 智能文件夹、色环、浏览器剪藏、批注裁剪、多库切换 |
| 3 | 瀑布流+虚拟滚动、粘贴导入、视频封面、撤销、查重、设置页/监控、快捷键 |
| 4 | 按 Web Interface Guidelines 全量 UI 重构（设计令牌/无障碍/动效/确认弹窗） |
| 5 | 改名「拾光素材库」+ 图标生成 + electron-builder 打包发布 |
| 6 | 修复：图库空态切换导致 ResizeObserver 未绑定、图片不显示的 Bug |
| 7 | UI 美术升级：SVG 图标库替代 Emoji、截图走查工具 .ui-shot |
| 8 | 修复：编辑器 canvas 仅 ready 时挂载导致 viewRef 为 null、打开即卡死（截图走查发现，0.1.0 已携带该 Bug） |
| 9 | UI 风格切换为手绘手账风（后被替代） |
| 10 | UI 风格切换为死亡搁浅终端风：墨黑+终端青、切角构件、等宽数据字、对角框线选中态；适配原画/概念图收藏场景 |
| 11 | 叠加故障艺术（RGB 撕裂/乱码解码/故障入场）与像素艺术（阶梯角/像素星点/空态像素画）；截图工具改 offscreen 渲染 |
| 12 | 冷色化：绿松石青→冰蓝（205°）、蓝黑底、星级去琥珀改银蓝、故障动效降噪（5.4s 周期/位移抖动不变色）、移除方框「拾」标 |
| 13 | 对标 Eagle 功能补全：悬停预览、汉明相似搜索（查重归组+以图搜图）、多级文件夹树、PSD 缩略图（ag-psd）、批量标签 |
| 14 | 自动更新：electron-updater 全链路（状态机/确认下载/进度/重启安装），本地双版本端到端验证通过（0.2.0→0.2.1），版本号升至 0.2.1 |
| 15 | 更新源迁移 GitHub Releases（yuanzhoucanxiang/shiguang-materials）；安装包名改 ASCII 规避附件名剥离问题；已装机客户端完成切换 |
| 16 | 更名「拾光档案馆」+ 冰蓝终端风新图标；Shift 范围多选；监控文件夹递归子目录；发布 v0.2.2 |
| 17 | 品牌升级：更名 LUMEN（光的单位，呼应拾光），图标改 L+终端光标几何标；浏览器扩展改 LUMEN Clip；发布 v0.3.0 |
| 18 | 预览缩放/拖动、悬停播放 GIF/视频/音频、瀑布流/网格/列表布局切换、批量星级、未标注与日期筛选；发布 v0.3.1 |
| 19 | 热修：回收站等拥挤视图工具栏按钮文字竖排问题（按钮全局 nowrap + 工具栏横向滚动）；发布 v0.3.2 |
| 20 | 回收站批量操作改纯图标按钮（恢复/永久删除/清空），清理 folders.length>=0 恒真遗留；发布 v0.3.3 |
| 21 | 标签颜色+标签分组（tag_groups 迁移/右键色板/移动到分组）；智能文件夹加构图/颜色数量/未标注；发布 v0.4.0 |
| 22 | 剪藏区域截图/长截图/批量过滤、导出文件夹与 ZIP（纯 JS ZIP 写入器）、fontkit 字体样张预览；发布 v0.5.0 |
| 23 | 热修：工具栏 overflow-x 裁剪导致格式/颜色等弹层不可见（Popover 改 fixed 定位逃出溢出上下文）；发布 v0.5.1 |
| 24 | 系统体检：新增 CDP 集成测试（.ui-shot/itest.cjs，真实库 13 项 IPC 断言全过）；修复导出覆盖同名文件、剪藏临时文件泄漏、工具栏滚动弹层脱节；发布 v0.5.2 |
| 25 | 更新卡片展示 Release Notes（updater 透传 releaseNotes，卡片与重启确认框均显示）；发布 v0.5.3 |
| 26 | 侧栏分区折叠（文件夹/智能文件夹/标签可折叠，展开区平分空间+独立滚动，状态存 localStorage）；发布 v0.5.4 |
| 27 | 数据安全与可观测性：启动自动备份 `library.db.bak`（better-sqlite3 backup 在线热备）；设置页新增「备份数据库」与「导出完整库 ZIP」（复用纯 JS ZIP 写入器，含原图+缩略图+db）；新增 `logger.ts` 文件日志系统（`userData/logs/main.log`，2MB 轮转保留 2 份，`uncaughtException`/`unhandledRejection` 全局兜底）；importer 9 处静默 catch 全部补日志（外层兜底记文件名+完整错误，PSD/字体/视频帧/缩略图分级别 warn/debug）；`ImportResult` 增 `failedFiles`，导入失败时 toast 显示「⚠ 失败 N 张」；watcher/editor/library 的 catch 补日志；发布 v0.5.5 |
| 28 | 导入性能与一致性：`importOne` 拆为两阶段——`prepareOne`（复制+缩略图/主色/dHash/视频封面计算，可并发）+ `commitBatch`（事务原子写 DB+metadata.json）；自写 `mapWithConcurrency` 并发池（按 `os.cpus().length` 并发，不引入新依赖）；better-sqlite3 `db.transaction()` 包裹整批 INSERT，任一失败整批 ROLLBACK（已复制文件保留，下次 isDuplicate 判重）；watcher/clipServer 单文件调用自动退化为串行，签名不变零改动；发布 v0.5.6 |
| 29 | 编辑器非破坏性编辑：`applyEdit` 不再覆盖原图，编辑结果写入 `{id}.edited.{ext}`（原图 `{id}.{ext}` 永久保留）；`assets` 表增 `edited` 列（ensureColumns 增量迁移）；`assetPaths` 查 `edited` 列，已编辑时优先返回 edited 文件路径（`asset://`/`shell:showItem`/`copyImage`/导出全部自动指向编辑后版本，零改动）；新增 `revertEdit`--删除 edited 文件 + 从原图重生成缩略图/主色/尺寸 + `SET edited=0`；右键菜单 `edited===1` 时显示「恢复原图」（ConfirmDialog 确认）；`assetThumbUrl`/Editor/Preview/Gallery/DupeModal 的图片 URL 拼 `&e={edited}` 版本戳强制刷新浏览器缓存；extractColors 统一传缩略图 buffer 与导入管线一致；发布 v0.5.7 |
| 30 | 检索与性能：文件夹视图改递归 CTE 联动子文件夹（`WITH RECURSIVE subtree`，看父文件夹时同时看到子孙文件夹的素材）；构图筛选（横/竖/方）与颜色数量筛选从内存过滤下推到 SQL WHERE（`width > height` / `json_array_length(colors) <= ?`），避免全表扫描后内存二次过滤；新增 `idx_assets_ext`/`idx_assets_star`/`idx_assets_width_height` 索引；EXIF 元数据：新增 `exif.ts` 极简解析器（无第三方依赖，解析 IFD0+ExifIFD 提取相机型号/拍摄时间/光圈/快门/ISO/焦距），导入时读取存 `exif` 列，Inspector 展示；发布 v0.5.8 |
| 31 | 视频故事板：导入视频时额外提取 4 帧（10%/35%/60%/85%）拼成 2×2 网格 `storyboard.jpg`（ffprobe 取时长 + ffmpeg seek 提帧 + sharp composite 拼接）；`asset://` 协议支持 `t=s` 加载故事板；preload 加 `storyboardUrl`；悬停预览视频时优先显示故事板图，onError 回退到视频播放；支持 mov/mkv/avi/wmv/m4v 全视频格式悬停预览；发布 v0.5.9 |
| 32 | 渐进查重：`findDuplicates` 支持可调阈值参数 `maxDistance`（默认仍 6）；查询按 `ext` 排序后分组比较，只在相同扩展名内做 O(n²) 汉明距离比较，大幅减少跨格式的无效比较次数；IPC `assets:findDupes` 与 preload 支持传入阈值；发布 v0.6.0 |
| 33 | CDP 全链路回归 + 热修：新增 `.ui-shot/itest-new.cjs` 专项验证（15 项断言，覆盖备份/SQL 下推/查重阈值/EXIF/edited 字段/非破坏性编辑/恢复原图，CDP 驱动渲染进程真实断言，全过）；发现并修复**编辑后扩展名变化导致原图路径错乱**的 bug：`applyEdit` 把 `ext` 更新为新格式，但原图文件名仍是旧格式，导致 `revertEdit` 找不到原图、edited 文件丢失时 asset:// 404——修复为 `ext` 恒指向原图格式，编辑版格式单独存 `edited_ext` 列（ensureColumns 增量迁移，assetPaths/applyEdit/revertEdit 适配，兼容旧数据回退 ext）；EXIF 解析器单测通过（手工构造含 ExifIFD 的 buffer，FNumber/ISO/FocalLength 全字段解析正确；sharp 生成测试图不支持写 ExifIFD 数值字段属测试图局限，真实相机照片字段齐全）；文件夹递归 CTE 实测通过（父/子文件夹造数据验证联动后清理）；发布 v0.6.1 |
| 34 | 真实视频全链路验证 + 三个视频相关 bug 热修：①**ffprobe 不存在**——ffmpeg-static 只打包 ffmpeg.exe 无 ffprobe，`getVideoDuration` 改用 `ffmpeg -i` 解析 stderr 的 Duration 行；②**EPERM 导入失败**——Windows Defender 实时扫描短暂锁定 ffmpeg 刚生成的临时帧文件，`rmSync` 抛 EPERM 导致整个导入失败（finally 清理失败未捕获），新增 `rmSafe` 重试删除（5 次 ×150ms，仍失败只记 debug 不阻断），导入不再失败；③**视频不能播放（MEDIA_ERR_SRC_NOT_SUPPORTED）**——`asset://` 协议未注册 privileged scheme，`<video>` 无法播放自定义协议内容（图片不受影响所以此前未暴露），修复为模块顶层 `protocol.registerSchemesAsPrivileged({ scheme:'asset', privileges:{ standard, secure, supportFetchAPI, stream } })`，同时 `protocol.handle` 改为手动构造响应：MIME 映射表 + Range/206 流式响应（`Readable.toWeb` 转换 Node stream，支持视频 seek 拖动进度条）；实测视频播放 ✓（1470x630 h264）seek ✓ 故事板加载 ✓（512x288 2x2 网格）；发布 v0.6.2 |
| 35 | 视频封面 + 更新说明格式：**视频卡片封面 = 故事板四宫格**——`assetThumbUrl` 的视频格式白名单补齐（此前视频不在图片白名单内导致封面从未显示，只显示格式图标），视频优先返回 storyboard URL（`t=s`），卡片 `onError` 二级回退（短视频无故事板 → 首帧缩略图 → 格式图标）；**更新说明分类分点渲染**——新增 `UpdateNotes.tsx` 组件，解析 GitHub Release notes 的「分类标题 + `·` 条目」格式（空行分隔区块，分类加粗 + 圆点缩进），更新卡片与下载确认框均按分点展示；README 补充 notes 格式约定（`✨ 新功能` / `🐛 修复` / `⚙️ 优化` 建议分类）；解析逻辑 4 用例单测通过（标准/纯条目/旧格式兼容/空分类）；发布 v0.6.3 |
| 36 | 一键发布脚本 `scripts/release.cjs`：版本号 bump（patch/minor/major，`--bump` 指定）→ notes 生成/校验 → `npm run build:win` 打包 → `gh release create` 上传安装包+blockmap+latest.yml → git tag + push，一条命令完成发布；notes 来源：优先读 `notes.md`（存在即用），不存在则从 `工作日志.md` 最后一条里程碑**启发式生成分点草稿**（按`；`/`。`切条目、按关键词自动分类 修复→🐛/新增→✨/其他→⚙️、去 `**` 加粗与「发布 vX.Y.Z」残留、长条目取「——」前主题），写入 notes.md 供编辑；发布前**格式校验**（每条 `·` 条目前必须有分类标题，不合规中止）；`--dry-run` 预览草稿+校验+命令不执行任何操作、`--no-git` 跳过 tag/push；踩坑：JS 正则 `$` 不匹配 CRLF 的 `\r`（里程碑行正则须用 `\|\s*$` 收尾）；发布 v0.6.4 |
| 37 | 发布 v0.6.4（汇总里程碑 30-36 全部改动：库备份/日志系统/导入并发事务/非破坏性编辑/文件夹递归/EXIF/查询优化/查重/视频故事板+播放/视频封面/更新说明分点/发布脚本，17 条分类 notes）；发布过程修复脚本两处问题：①`gh release create` 未指定 `--repo` 导致 Release 误建到源码仓库 lumen（gh 默认用当前 remote）——已加 `--repo yuanzhoucanxiang/shiguang-materials` 并清理误建 Release/tag；②git tag push 冲突处理；验证：发布仓库 3 资产完整（安装包 146MB + blockmap + latest.yml）、latest 指向 v0.6.4、lumen 源码仓库无 Release 残留 |
| 38 | **重大热修：electron-builder 打包缺应用代码**——`.gitignore` 里的 `out` 被 electron-builder 当作排除规则，asar 内只有 node_modules + .ui-shot 垃圾缓存、无 `out/main|preload|renderer`，导致 0.6.4/0.6.5 安装包残缺（用户视频播放异常/功能缺失但 dev 正常，极难定位）；修复 `electron-builder.yml` files 显式包含 `out/**` + 排除 `.ui-shot/**`；验证修复后 asar 含完整应用代码，打包版 CDP 实测视频播放 ✓；附带安装包瘦身 153→136MB；发布 v0.6.6（首个完整安装包） |
| 39 | 预览视频"静帧"热修：双击预览的 `<video>` 无 `muted`，Chrome/Electron 自动播放策略阻止有声视频 autoPlay（首帧显示但不播放，且 CDP 裸 video 测试会因"video-only background media paused"误导排查）；Preview.tsx video 加 `muted` + `playsInline`（静音自动播放合规，与悬停预览一致）；发布 v0.6.7 |
| 40 | **视频"静帧"真根因热修**：`assetThumbUrl` 改为视频返回故事板 URL（里程碑 35）后，Preview.tsx 用 `assetThumbUrl(asset)` 判断 `isImage`——视频被误判为图片，预览显示故事板静帧而非播放（0.6.6/0.6.7 的 muted 修复无效的深层原因）；修复为 Preview 用独立图片扩展名集合判断 `isImage`（视频走 video 分支）；CDP **真实双击路径**验证：双击视频卡片 → video 元素 → 自动播放 ✓（t 前进、readyState=4）；排查教训：CDP 裸 video 测试 ≠ 真实 UI 路径，必须驱动真实交互验证；发布 v0.6.8（首个视频预览真正可播的版本） |
| 41 | **启动增量同步**（类 Eagle）：`watcher.ts` 新增 `syncOnStartup`--软件关闭期间监控文件夹新增的文件，下次启动自动检测并导入；复用 `importFiles` 的查重逻辑（name+size 已入库的 skip），只导入新增；`collectFiles` 递归展开 watchDirs，`importFiles` 并发+事务入库；index.ts 在 `syncWatchers` 后调用 `syncOnStartup`，有导入则 `webContents.send('clip:imported')` 通知刷新；发布 v0.6.9 |
| 42 | **AI 智能重命名 + 自动打标签**（对标 Eagle）：新建 `aiRename.ts`--OpenAI 兼容格式（智谱 GLM-4V / 通义 / Ollama / OpenAI 通用），多选素材 -> 读缩略图 base64 + 元数据（原名/尺寸/标签）-> 调 `chat/completions` 一次生成 `{name, tags}` JSON -> 直接应用（改名 `updateAsset` 保留扩展名 + 标签追加 `addTagToAssets`）；无预览步骤，批量自动化（标签追加式可删，改名可撤销）；3 并发限流防 API 限流，单张失败不阻断，JSON 解析容错（去 ```` ```json ```` 包裹 + 提取 `{...}`）；设置页加 AI 配置区块（Base URL + API Key 密码框 + 模型 + 测试连接），key 存主进程 config.json 脱敏返回（只给末 4 位，不进渲染进程）；入口：Gallery 右键菜单「AI 智能处理」+ Inspector 批量面板按钮；App.tsx 进度卡片订阅 `onAiProgress` 实时显示 done/total；repository 新增 `getAssetById`（含标签）；真实 AI 测试通过（3 张图全部成功，生成描述性中文名+追加标签）；发布 v0.7.0 |
| 43 | AI 体验增强 + 侧栏排序：①**工具栏一键 AI 按钮**（选中素材后工具栏直接点「AI 处理」，不用进右键菜单）；②**导入后自动 AI**（设置页开关，导入完成自动对新素材跑 AI，`autoAiAfterImport` 取前 N 条按 imported_at 排序的新素材调 aiProcess）；③**AI 标签库优先复用**--prompt 传入已有标签库（`listTags` 前 50 个），让 AI 优先从已有标签选用，避免同义标签泛滥（如"夜景"和"夜晚场景"）；④**侧栏按素材数量降序**--文件夹树/智能文件夹/标签（含分组内）统一按 count 降序 + 名称兜底；发布 v0.7.1 |
| 44 | **AI 处理对话框 + AI 标签自动归组**：①新建 `AiDialog.tsx` 三态对话框--配置（处理范围：选中/全部/无标签/未命名 + 处理内容：改名/打标签 + 每张最多标签数 3/5/8）-> 进度（onAiProgress 订阅，done/total/失败数）-> 结果列表（每行缩略图+旧名划线+新名+新增标签 chips，可勾选撤销单条，撤销=恢复 oldName + setAssetTags 移除 addedTags）；②**AI 标签自动归组**--`aiProcessBatch` 内按标签名聚合后 `createTag` + `assignTagToGroup` 归入「AI 标签」组（同名复用），批量打标从逐条改为一标签一次（减少 DB 操作）；③**未命名判定** `isUnnamedName`（相机默认名 IMG_/DSC_/日期串/纯数字/短名）；④新增 IPC `ai:countCandidates`（对话框显示"将处理 N 个"）+ `ai:resolveScope`（范围展开为 ids，未命名判定与 count 一致）；⑤三个入口（Toolbar/Gallery 右键/Inspector 批量面板）统一改为 `openAiDialog`（store 加 aiDialogOpen 状态），App.tsx 挂载 AiDialog；发布 v0.7.2 |
| 45 | **AI 处理全链路实测 + 修复**：CDP 驱动真实 UI 走完整流程（范围计数 选中1/全部652/无标签263/未命名19 全部正确；真实智谱 GLM-4V 调用成功——`madao-.jpg`→`奇幻角色设计展示.jpg` 新增 4 标签；「AI 标签」组自动创建、4 标签归组、3/4 复用已有标签；单条撤销名字恢复+标签移除全部生效）；修复两处：①`revertOne` 撤销时 `queryAssets({limit:100000})` 全量拉库查单个素材——新增 IPC `assets:get`（repository 已有 `getAssetById` 未暴露），撤销改按 id 精确查询；②撤销全部后空列表文案误显示「AI 判断原名/标签已合适」（实际是用户自己撤销的）——新增 reverted 计数，区分显示「已撤销 N 条 AI 处理更改」；附带 `scripts/cdp-eval.cjs` 通用 CDP 求值工具（node scripts/cdp-eval.cjs "表达式"）；发布 v0.7.3 |
| 46 | **修复重启/监控重新导入已删除或 AI 改名素材的 Bug**：根因双重--①`isDuplicate` 按 name+size 查重且过滤 `deleted_at IS NULL`,AI 改名只改 DB name 不动源文件,改名后查重失效(实测 2 组重复均为 AI 改过的中文名);软删进回收站的记录也不参与查重,重启扫描到源文件又导入。修复(用户确认方案:只改库内不改源文件 + 新增 tombstone 表):①**新增 `deleted_files` tombstone 表**(`db.ts`)记录已删文件 hash+size+name,`migrate()` 内回填当前回收站记录(幂等);②**`isDuplicate` 两阶段哈希查重**--name+size 活跃命中(快速路径,零开销)→图片预算 512 缩略图算 dHash(与已存储哈希同源)做 hash+size 活跃命中(AI 改名后 name 变内容不变→命中防重复)→可选 tombstone 命中(已删文件);`prepareOne` 重构:图片先从源文件预算缩略图+哈希做二次查重,通过后复用 thumbBuf 写盘(不二次生成);③**tombstone 写入/清除**(`repository.ts`)--`deleteAssets` 软删/永久删都写 tombstone,`restoreAssets` 清除对应 tombstone(恢复后可重导入),永久删除保留 tombstone(持续阻止重导入);④**导入路径区分**--`ImportOptions.checkTombstone`:监控/启动同步(syncOnStartup+handleFile)设 true(尊重删除记忆),用户主动导入(对话框/拖拽/剪藏)不设(允许重新导入已删文件);⑤`libraryStats` 加 `tombstones` 计数(诊断用);CDP 实测 6 项全过:软删→touch 源文件不重导✓、AI 改名→touch 不产生重复(哈希命中)✓、永久删→touch 不重导✓、restore→tombstone 清除✓、tombstone 计数正确✓、已有 2 组重复已清理✓;发布 v0.7.4 |
| 47 | **AI 打标签全面优化**:四方向整合--①**预览审核模式**:拆 `aiProcessBatch` 为 `aiSuggestBatch`(只生成不写 DB)+ `aiApplySuggestions`(用户审核后落库),AiDialog 新增 preview 阶段(可编辑建议列表:每行缩略图+可改文件名 input+分类着色标签 chip 可点 × 删除+勾选/取消整行),config->processing->**preview**->result 四态;②**标签去重清洗** `cleanTags`:去空格/括号/引号、全角转半角、过滤无意义词(无/未知/图片等)、同义词归并(匹配已有标签库大小写不敏感+去空格)、同 name 去重;③**Prompt 调优**:temperature 0.7->0.3(更稳定)、新 prompt 明确分类要求(scene/style/subject/color)+few-shot 示例、标签库 hint 从 50 扩到 100 个用逗号分隔、只打标签时 prompt 要求 `"name":""` 省 token、`extractJson` 升级解析 `tags:[{name,cat}]` 兼容旧 `tags:["a"]`;④**标签智能归组细分**:AI 返回标签带分类,按分类自动归入 `AI-场景`/`AI-风格`/`AI-主体`/`AI-色调` 子组(`CATEGORY_GROUPS` 映射),**不覆盖已有分组**(只对 group_id=null 的新标签归入 AI 子组,修复此前强制迁移已有标签的副作用);新增 IPC `ai:suggest`+`ai:apply`+ preload 对应;`aiProcess` 保留(内部改为 suggest+apply 链,供 autoAiAfterImport 无感自动化);CDP 实测:预览阶段出现可编辑列表(标签 4 分类着色)✓、删标签后应用只保留选中标签✓、新标签归入 4 个分类子组✓、AI 100% 复用已有标签库(同义归并生效)✓;发布 v0.7.6(注:首次 release 因 notes 生成器不兼容半角分号崩溃,修复后重跑 bump 跳过 0.7.5 直接发 0.7.6;release.cjs 已修复兼容全角/半角分号) |
| 48 | **AI 打标签完善:用户自定义优先标签 + 历史同义词合并工具**：①**优先标签**--`tags` 表加 `priority` 列(ensureColumns 迁移,0=普通/1=优先),侧栏右键菜单加「设为优先标签 ⭐」/「取消优先」,优先标签侧栏带 ⭐ 标记;AI 逻辑:prompt 优先标签单独列出(「这些是用户最常用的标签,只要内容匹配就优先选用它们」),`cleanTags` 同义词归并升级为**优先标签最优先**(先匹配 priority=1 的标签,命中强制归并为优先标签,未命中再匹配常规标签库),AiDialog 预览中优先标签 chip ⭐ 高亮;②**标签合并工具**--`mergeTags(sourceId, targetId)`(db.transaction:源标签素材全部打上目标标签 → 删除源标签关联 → 删除源标签,原子完成),新建 `MergeTagDialog.tsx`(搜索框 + 目标标签列表按 count 降序/优先标签置顶带 ⭐ + 选中确认),侧栏右键菜单加「合并到其他标签…」;③**性能优化**--`aiSuggestBatch` 一次 `listTags()` 获取 Tag[] 复用(含 priority),传入 `suggestForAsset`,消除此前每张图 N 次 DB 查询;CDP 实测:设为优先后侧栏 ⭐ 显示✓、右键菜单两项✓、MergeTagDialog 列表排序+搜索✓、mergeTags 原子合并(2+2=4 素材迁移+源标签删除)✓、优先标签置顶✓;发布 v0.7.7 |
| 49 | **更新推送诊断 + updater 重试机制**：用户反馈"没更新推送"，全链路诊断：①服务端正常(v0.7.7 latest.yml 可达、资产齐全、latest 标记正确)；②用户机器装的是 v0.7.4，手动检查**完全正常**(检测到 0.7.7 带完整 notes、下载、安装全部成功,已升级到 v0.7.7)；③**根因=自动检查只在启动后 8s 做一次且失败静默无感知**——应用一直开着不重启永远不检查;网络抖动时 electron-updater 的检查请求**无限挂起**(无 error 事件、永不超时、永不重试);修复 `updater.ts`：**60s 超时保护**(Promise.race 强制超时,超时视为失败)→ **递增重试计划**(8s/30s/2min/10min/30min,直至成功或耗尽)→ **日志落盘**(console.log 改 logger,main.log 可查每次检查状态,排查不再抓瞎);CDP 实测:checking→失败→"30s 后重试"→二次检查→"120s 后重试" 完整重试链✓;踩坑:①asar extract-file 才是可靠验证(asar list 反斜杠转义易误判"缺代码")②在实例运行时打包会 EBUSY 失败导致旧 asar 残留,测试前必须先杀实例;发布 v0.7.8 |
| 50 | **设置弹窗溢出修复**:用户反馈"设置界面 UI 拉得有点高,看不到上边的内容"——根因:SettingsModal 对话框无高度约束也无滚动容器,AI 配置区块(Base URL/模型/API Key/测试连接)加入后内容总高超出屏幕,顶部标题与「导入方式」被推出视口且无法滚动;修复:对话框改 `flex max-h-[85vh] flex-col`,内容区包进 `modal-scroll min-h-0 flex-1 overflow-y-auto`(space-y-5 统一间距,去掉各区块 mb-5/mt-5),标题与「完成」按钮固定在滚动区外(底部加分隔线);CDP 实测:对话框完全在视口内(fitsViewport=true)、标题 77px 可见、滚动容器存在(内容超出 265px 可滚)、导入方式顶部区块可见;发布 v0.7.9 |
| 51 | **AI 智能搜索(自然语言找图)**:三阶段混合方案--①**语义扩展**(1 次纯文本调用,搜索词→`{keywords,tags}`,tags 限从标签库选,prompt 强化「无关时 tags 返回空数组不要强行匹配」);②**SQL 宽松候选**(新增 `repository.searchAssets(anyTagIds,keywords)` OR 语义:任一标签命中或 name/comment LIKE,现有 queryAssets 的 tagIds 是 AND 语义不适用;候选按命中扩展标签数预排);③**视觉精排**(新建 `aiSearch.ts`,候选前 36 张按 6 图/批并发 3 路发给 GLM-4V 打分 0-10,分数阈值 ≥4 过滤,全被过滤时兜底前 5;单批次失败静默容忍,全失败降级回阶段 2 顺序);**性能调优踩坑**:单请求多图耗时超线性(12 图/批≈50s vs 6 图/批≈3-10s),改小批次+并发 3 后整次搜索从 4 分钟降到 14 秒;UI:Toolbar 搜索框右侧 sparkles 切换钮(AI 模式 placeholder/边框高亮/Enter 触发/Esc 退出/搜索中 spinner+进度浮层),Gallery 横幅「AI 搜索:查询词 N 个结果 [退出]」(复用 similarTo 横幅模式),store 加 `aiSearch` 状态 + `setAiSearchResults`/`clearAiSearch`,refreshAssets 在 AI 搜索时提前返回不覆盖结果,setView/setSimilarTo 清空 aiSearch;新增 IPC `ai:search` + `ai:searchProgress` 推送 + preload `aiSearch`/`onAiSearchProgress`;CDP 实测(真实 GLM-4V):「暗黑哥特城堡」14.2s→7 条高质量结果(暗黑氛围/阴森场景类)✓、「雾中城堡」9 条✓、「海边度假」1 条✓、无意义查询 tags=[] 0 结果 1.1s✓、2 批次 fetch failed 容错降级不崩溃✓、横幅/退出/视图恢复✓;按新约定**未发布**(等用户指示汇总新版本) |
| 52 | **批量标签管理视图**(`TagManagerDialog.tsx`):侧栏「标签」分区标题加「管理」入口(扩展 `SectionHeader` 加可选 `onManage` prop);对话框:搜索过滤 + 按分组聚合列表(组内 count 降序)+ 多选 checkbox;**批量操作栏**(选中≥1 显示):合并所选(内层目标选择列表,count 降序/优先带⭐)、移到分组(内层分组列表含未分组)、设为优先/取消优先(全优先时自动反转为取消)、删除(ConfirmDialog 确认);单行快捷:⭐ 点击切换优先、pencil 行内重命名(Enter 确认/Esc 取消);执行层复用现有 IPC 循环调用(mergeTags/assignTagToGroup/setTagPriority/deleteTag,标签量小无需新 repository 函数),每次操作后 refreshTags+refreshTagGroups+refreshAssets 三连刷新;CDP 实测:搜索过滤✓、批量合并(A+B→C 素材迁移+源删除)✓、批量设为优先✓、批量移组(AI 标签)✓、批量删除(确认弹窗)✓、行内重命名✓;按新约定**未发布** |
| 53 | **批量导出增强 + 素材备注 Markdown + AI 标签负向 + 预览 1:1 + 更新持久入口 + 快捷键自定义 + 日志导出 + 集成测试**(一次批量推进 8 项):①**导出增强**--新建 `ExportDialog.tsx`(导出方式:文件夹/ZIP + 命名模板:原名/标签_原名/标签_序号/原名_序号 + 按标签分文件夹勾选),右键菜单两个导出项合并为一个「导出…」;`exporter.ts` 支持命名模板(`buildName`)+ 按第一标签分文件夹(`subDir`+`safePathSegment` 清洗非法字符)+ ZIP 子目录路径,IPC/preload 透传 `ExportOptions`;②**素材备注 Markdown**--新建 `Markdown.tsx` 极简渲染器(标题/加粗/斜体/行内代码/链接/列表,零依赖,链接点击走 openExternal),Inspector 注释区改编辑/预览切换(eye/pencil),textarea 加高(支持 Markdown 语法提示);③**AI 标签负向(排除)**--`tags` 表加 `excluded` 列,侧栏右键「排除标签 ⊘(AI 不使用)」,侧栏排除标签置灰+⊘ 标记,`cleanTags` 直接丢弃排除标签,AI prompt 加「绝对不要使用这些标签」;④**预览 1:1**--Preview 右下角「1:1」按钮(图片实际像素=屏幕像素,imgRef 算 naturalWidth/clientWidth,已是 1:1 时再点复位);⑤**更新已就绪持久入口**--下载完成弹窗关闭后不再丢失(此前 upd 直接清空导致无法再安装),改为显示右下角「新版本 vX 已就绪·重启安装」脉冲角标,点击重新打开确认弹窗;⑥**快捷键自定义**--新建 `renderer/shortcuts.ts`(localStorage 持久化,预览/全选/撤销删除三个动作可重绑,`eventToKeys`/`matchesShortcut` 工具),设置页新增「快捷键」区(录制式按钮:点击后按新键位,Esc 取消),App 键盘处理器改走映射表+`lumen:shortcuts` 事件重读;⑦**日志一键导出**--`logger.logFilePath()` + IPC `logs:export` + 设置页备份区「导出运行日志」按钮;⑧**集成测试固化**--新增 `.ui-shot/itest-ai.cjs`(6 项确定性断言:优先/排除/合并/tombstone 字段/注释搜索/preload API,无需真实 AI 调用),实测 6 PASS/0 FAIL;CDP 验收:ExportDialog 交互✓、Markdown 编辑器✓、1:1 按钮✓、快捷键设置区✓、日志导出按钮✓、排除菜单项✓;按新约定**未发布** |
| 54 | **HEIC/HEIF 支持(意外收获)**:排查发现项目依赖的 sharp 内置 libvips 已升级到 8.17.3 原生支持 HEIF 解码(此前工作日志记「HEIC 依赖 libvips 构建」为限制)——导入白名单 `IMAGE_EXTS` 本已含 heic/heif,缩略图生成早已可用;缺的是显示侧:卡片 `assetThumbUrl` 白名单补 heic/heif/ai,预览 `IMAGE_EXTS` 补 heic/heif(Chromium 不解码,onError 自动回退缩略图预览);端到端实测:sharp 生成真实 .heic 文件 → 监控自动导入 ✓ → 缩略图生成+加载 ✓(64x64 尺寸正确)→ 清理;**剩余两项目前不做的说明**:①大规模性能(dHash O(n²))——当前 500+ 素材实测秒级,无实际瓶颈,等万级规模出现再优化(YAGNI);②代码签名——需用户购买代码签名证书,属购买决策非开发项;至此本轮优化清单全部完成(除上述两项),共 4 个里程碑(51-54),按新约定**未发布** |
| 55 | **里程碑 51-54 改动全面代码审查 + 修复 6 处问题**:用户要求「先仔细审查代码质量再 debug」,逐文件审查 1660 行改动后修复:①**快捷键 Meta 键回归**——`shortcuts.matchesShortcut` 从 `(e.ctrlKey||e.metaKey)` 改为 `e.ctrlKey===wantCtrl`,导致 macOS Cmd+Z/Cmd+A 失效(旧版行为回归);修复为「绑定 Ctrl 的快捷键在 Mac 上同时匹配 Cmd,绑定单键时 Ctrl/Cmd 必须都未按下」;②**AI 搜索竞态**——搜索进行中用户切视图/退出,结果回来 `setAiSearchResults` 无条件覆盖新视图(真实竞态);修复:store 加 `aiSearchPending` 令牌,`setView`/`setSimilarTo`/`clearAiSearch` 清 pending,`setAiSearchResults` 校验 `pending===query` 才生效,过期结果丢弃;③**AI 搜索无超时**——API 挂起时 UI 无限转(updater 已修过同类问题,aiSearch 遗漏);修复:`chat` 加 AbortController 60s 超时;④**exporter 跨子目录重名**——`uniqueName` 的 taken 集合全局共享,不同标签子目录同名文件被误加 (1) 后缀;修复:按目标目录分别维护 taken(`Map<dir,Set>`);⑤**expandQuery 标签库无上限**——标签多时 prompt 超 token;修复:按素材数降序截取前 200 个;⑥**TagManagerDialog 合并目标不随搜索过滤**——UX 不一致;修复:mergeCandidates 加 keyword 过滤;集成测试补 1 条断言(7 PASS/0 FAIL),CDP 实测竞态防护(搜索中切视图→结果被丢弃→无横幅)✓;按新约定**未发布** |
| 56 | **发布 v0.8.0(major 里程碑)**:汇总里程碑 51-55 全部改动(9 项功能 + 6 处审查修复),`--bump minor` 从 v0.7.9 升 v0.8.0;手动编写分类 notes(✨ 7 条新功能/⚙️ 4 条优化/🐛 4 条修复);发布流程一次成功(打包→gh release→git tag+push 全程无网络波折,notes.md 用后自动清理);验证:3 资产齐全(exe 143MB + blockmap + latest.yml)、latest=true、v0.7.9 降为旧版、notes 内容正确;新增内容:AI 智能搜索/批量标签管理/导出模板+按标签分文件夹/Markdown 备注/预览 1:1/更新就绪持久角标/快捷键自定义/AI 标签负向/HEIC 支持/日志导出/集成测试 |
| 57 | **全项目审查优化(9 项)**——①**AI 请求统一超时**--新建共享 `main/aiClient.ts`(chat 带 AbortController 60s 超时 + mapWithConcurrency 并发池),`aiRename.suggestForAsset` 与 `testAiConnection`(20s)补上超时保护(此前仅 aiSearch 有,网络挂起会无限等待),aiRename/aiSearch 删除本地重复实现统一复用;②**剪藏服务鉴权**--`clipServer.ts` 要求 `X-Lumen-Client` 头(403 拒绝无头请求),去除 `Access-Control-Allow-Origin:*` 全开 CORS(网页跨域无法携带自定义头,防任意网页向素材库注入;MV3 扩展持 host_permissions 可正常携带),浏览器扩展 5 处 fetch 同步带头;③**dist 历史安装包清理**--手动清 2.29GB→0.13GB(只留 v0.8.0),`release.cjs` 发布成功后自动清理旧版 exe/blockmap;④**打包品牌修正**--快捷方式/卸载名改 LUMEN,productName/executableName 保持不变并注释原因(改动会破坏老用户更新链路:双安装+检测不到运行中 exe);⑤**README 版本号** v0.5.4→v0.8.0;⑥**npm 镜像配置**--.npmrc 旧键保留(npm 10 有效)+README 补充完整 env 方案(npm 11+ 将停用旧键);⑦**安全加固**--生产构建注入 CSP meta(electron.vite.config.ts `inject-csp` 插件,dev 不注入避免 React Refresh 内联 preamble 被拦),渲染进程 `sandbox:true`(preload 仅 contextBridge/ipcRenderer,沙箱兼容);⑧**测试接入 npm**--新建 `scripts/itest.cjs` 运行器(启 dev→顺序跑 .ui-shot/itest*.cjs→关 dev),`npm run test:itest`/`test:itest:ai`;⑨验证:typecheck✓、build✓、CSP 已注入、CDP itest-ai 7 PASS/0 FAIL、剪藏 403/200 鉴权✓、UI 32 图渲染✓;按新约定**未发布** |
| 58 | **白板功能(无限画布,借鉴 MOTZ 白板)**:用户提供 motz-whiteboard 参考项目(Eagle+PureRef 结合体),研究其核心实现后为 LUMEN 加白板第一阶段:①**数据层**--`boards` + `board_items` 表(SQLite,board_items 存 x/y/width/height/z/text,asset_id 关联素材),repository 9 个函数(listBoards/createBoard/renameBoard/deleteBoard/listBoardItems/addBoardItem/updateBoardItem/deleteBoardItem/bringBoardItemToFront),IPC `boards:*`/`board:*` + preload 对应;②**界面**--侧栏新增「白板」分区(新建/重命名/删除,折叠状态持久化),ViewType 加 `{type:'board',id}`,App 主区域条件切换(Gallery ↔ BoardView),store 加 boards/boardItems + refreshBoards/refreshBoardItems;③**BoardView.tsx**(新建,核心画布)--参考 MOTZ 的 frame+surface 两层结构:`translate3d+scale` 视口变换,滚轮缩放以光标为锚点(rAF 逐帧更新 DOM 不触发 React 重渲染),空格+拖拽/中键平移,`canvasPointFromClient` 坐标换算,点阵背景;元素:图片(asset:// 协议缩略图,height=0 按宽高比自动)/视频(故事板)/文字 note(textarea),拖动/调整大小手柄/Delete 删除/点击置顶(z)/选中高亮;从图库拖素材进白板(复用 `application/x-eaglelike-assets` MIME,drop 错开摆放);自动排列(grid 3 列/row/column);工具栏:重命名/文字/排列/缩放百分比+复位;**实测踩坑 4 个**:①白板视图时 assets 为空导致 asset 元素被渲染成 note(修复:setView board 时同时 refreshAssets);②空格平移用 state 有更新延迟(修复:spaceDownRef);③window keydown 的 e.target 无 closest 抛错中断监听(防御 typeof closest==='function');④CDP 合成事件 setPointerCapture 抛异常(try-catch 包裹);CDP 实测:建白板✓ 视图切换✓ 拖素材进白板✓ 元素拖动持久化✓ note 文字✓ 网格排列(72,72/312,72)✓ 滚轮缩放(1→1.16 光标锚点)✓ 空格/中键平移✓ Delete 删除✓ 右键菜单✓;按新约定**未发布** |
| 59 | **侧栏布局优化(白板分区拥挤)**：用户反馈左侧加了「白板」后非常拥挤——根因:白板与其他三大分区(文件夹/智能文件夹/标签)同为  平分空间,4 个展开分区互相挤压;修复:白板分区改为**固定高度块**(,不再参与 flex-1 平分),**默认折叠**(只占 31px 标题行),展开时列表 **max-h-44(176px)内部滚动**(白板数量少,不需要大空间);实测:默认状态白板 31px,文件夹/智能文件夹/标签各 134/134/142px(比之前宽裕);展开 6 个白板时列表锁 176px 内部滚动,文件夹/标签仍 76/84px 不被挤压;按新约定**未发布** |
| 60 | **白板升级:独立创作参考工作区**——用户要求把白板当作与素材库平级的内置功能,主界面加专属入口+专属页面:①**视图状态机重构**--ViewType 移除旧 ,新增 (工作区入口)+  + (进工作区+切白板+刷新三件套),setView 离开工作区清 activeBoardId;②**主导航入口**--侧栏顶部导航(全部素材/已收藏/回收站旁)加「白板」项,点击进工作区(无白板自动建「默认白板」);③**BoardWorkspace.tsx(新建,专属页面)**--顶栏(白板名重命名/文字/网格横竖排列/操作提示)+ 左面板(白板列表:新建/切换/重命名/删除带确认;素材托盘:搜索防抖 300ms + 200 个缩略图网格,可拖到画布或双击加入)+ 右侧大画布;④**BoardCanvas.tsx(新建)**--从旧 BoardView 提取画布核心(frame+surface 两层、光标锚点缩放、空格/中键平移、元素拖动/缩放/Delete/置顶、拖放接收);⑤删除旧 BoardView.tsx;实测踩坑 3 个:①React onDoubleClick 在 CDP 合成事件下不触发(改用 onClick 双击检测,间隔<300ms,兼容性更广);②React onWheel 委托对 CDP 合成事件不触发(改原生 addEventListener + passive:false);③rAF 在 CDP/后台窗口不调度导致 transform 不更新(改同步应用 transform,浏览器自动合并高频渲染);CDP 实测:主导航入口✓ 工作区布局✓ 白板列表新建/切换✓ 托盘搜索+拖入✓ 双击加入(逻辑链路直调验证)✓ 缩放(1→1.35 锚点)✓ 空格/中键平移✓ 元素拖动持久化✓ Delete✓ 网格排列✓ 切回素材库✓;按新约定**未发布** |
| 61 | **UI 字体比例与密度调整(解决拥挤,对标 PureRef 极简理念)**——用户反馈界面太拥挤,且白板功能对标 PureRef(极简自由参考板)。审计发现 8 档字号(9-13px)过密、行距内边距紧、侧栏 4 分区拥挤;调整:①**字号体系三档化**--消灭 10px 以下文字:辅助(计数/徽章/状态)统一 11px,正文(导航/按钮/列表)12-13px,标题 11-16px;body 13px→13.5px、line-height 1.5→1.6;section-title 10px→11px(letter-spacing 0.18em→0.14em);按钮/输入框 font-size 12px→13px、padding 微增;滚动条 8px→10px;②**密度调整**--侧栏 w-60→w-64(240→256px),NavItem py-7px→py-2,列表 space-y-px→space-y-0.5,分区间距 mt-3→mt-4,右键菜单 py-1.5→py-2,空状态 11px→12px;工具栏 FilterButton py-1.5→py-2、间距 gap-2→2.5;Gallery 卡片名 10.5px→11px、LABEL_H 24→26、LIST_ROW_H 38→40、行辅助 10px→11px;Inspector 信息行 11px→12px、标签 chip 加 padding;白板工具栏 11px→12px、列表行 12px→13px、输入框 11px→12px(托盘角标 9px→10px 为唯一允许的 10px);CDP 验证:侧栏 256px✓ NavItem 35px✓ 分区更宽裕(文件夹/智能/标签 114/114/122px vs 此前 76/84)✓ 无 body 溢出✓ 工具栏 12 按钮全可见✓ 白板缩放/平移回归✓;按新约定**未发布** |
| 62 | **白板照抄 MOTZ 实现(split 分屏)+ 项目策划案**:用户要求照抄 motz-whiteboard 的白板功能实现,并新建项目策划案。①**split 分屏布局**(照抄 MOTZ main-pane-resizer)--素材库与白板同屏常驻,中间 resizer 可拖拽调宽(存 localStorage),viewMode split/board 全屏切换(工具栏按钮);App 启动自动打开第一个白板;②**素材卡片发送到白板**(照抄 MOTZ send-chip)--卡片右下角 chip 按钮,已发送显示 ✓(青底勾),再次点击提示「已在当前白板」;批量:Inspector 批量面板「发送到当前白板」按钮;③**发送位置算法**(照抄 MOTZ sendAssetsToBoard)--3 列网格错开(`96+(n*42)%260` 起点),按素材宽高比定尺寸(最大 280 宽),store 新增 sendAssetsToBoard action 统一落库;④**8 向缩放手柄**(照抄 MOTZ selection-handles)--nw/n/ne/e/se/s/sw/w 八个手柄,角拖动/边拖动,最小 40px;⑤**画布右键菜单**(照抄 MOTZ board-context-menu)--元素:从白板移除/网格/横向/纵向排列;空白:添加文本/排列;note:字体选择(5 种)+ 文本颜色(5 色+自定义,DB 新增 note_font/note_color 列);⑥**白板工具栏**(照抄 MOTZ board-toolbar)--当前白板下拉切换/新建/缩放滑块(range 0.1-4+百分比)/网格横竖排列/文字/全屏切换;⑦**侧栏**--移除主导航「白板」入口(白板常驻右侧无需入口),白板列表保留(切换/管理);⑧**新建 `项目策划.md`**--项目定位(素材库+创作参考白板)、已完成/进行中/规划中功能清单、主要参考项目(Eagle 素材管理/PureRef 白板极简/MOTZ 一体化)、技术架构、发布节奏、风险决策记录;删除 BoardWorkspace.tsx(被 BoardPanel 取代);CDP 实测:split 布局✓ 卡片发送✓ 已发送 ✓ 状态✓ 8 向手柄✓ 右键菜单(移除/排列/添加文本)✓ note 样式面板✓ 缩放滑块✓ 全屏切换✓ resizer✓ 启动自动打开白板✓ 画布缩放/平移回归✓;按新约定**未发布** |
| 63 | **白板框选多选(对标 MOTZ group-selection)**:①**框选**--空白处按下拖拽出现半透明选框(Shift 追加模式),松手按相交判定选中(画布坐标换算,视口缩放/平移下准确);②**组移动**--拖拽已选中元素整组同步位移,组外元素不动(原生 DOM style 实时更新,松手批量落库);③**组缩放**--多选时出现组包围盒+8 向手柄,拖手柄按包围盒等比缩放组内每个元素(坐标/尺寸按比例映射,最小 40px);④**批量落库**--新增 `updateBoardItems` 事务批量更新(repository+IPC `board:updateItems`+preload),组操作一次写库;⑤**Delete 删除选中组** / **Ctrl+A 全选** / Esc 取消;⑥**右键菜单作用于组**--多选时「从白板移除(N 项)/网格/横排/竖排排列」作用于选中组,note 字体/颜色批量应用;修复 2 个自测 Bug:①`finishMarquee` 先清 ref 再算矩形导致框选永不命中(改为先算后清);②`selectionBBox` useMemo 只依赖 boardItems 读 ref,选中变化不重算导致组包围盒不出现(改为依赖 state);新建 `.ui-shot/itest-board-multi.cjs` 专项测试(独立测试白板+重载刷新 store+合成指针事件),CDP 实测 10 PASS/0 FAIL:框选包围盒✓ 组移动同步位移+组外不动✓ 组缩放等比✓ Delete✓ Ctrl+A✓;按新约定**未发布** |
| 64 | **白板透明度 + 参考线(对标 PureRef)**:①**元素透明度**--`board_items` 加 `opacity` 列(ensureColumns 迁移,默认 100),右键菜单新增「透明度」区(25/50/75/100 预设按钮 + 10-100 滑块,实时显示百分比),多选时批量应用(复用 `updateBoardItems`),作用于图片/视频/文字(透明度加在内部媒体元素上,选中描边不淡化);②**参考线**--`boards` 加 `guides` 列(JSON 数组 [{x?,y?,horizontal}]),空白处右键「添加垂直/水平参考线」按光标位置落线,参考线可拖动(垂直沿 x/水平沿 y,画布坐标换算,松手持久化 `board:setGuides`),右键线身「删除参考线」;虚线冰蓝样式随画布缩放平移;修复 1 个自测 Bug:参考线拖动松手持久化时闭包里的 guides 过期(pointermove 的 setState 异步渲染,同一任务内 pointerup 拿到旧值)——改为 guidesRef 镜像 + 松手时从 drag 起点重算位置;新建 `.ui-shot/itest-board-opacity-guides.cjs` 专项测试,CDP 实测 8 PASS/0 FAIL:预设 50%✓ 滑块 75%✓ 多选批量 25%✓ 添加水平+垂直✓ 拖动持久化 y 499→549✓ 删除✓;回归 itest-board-multi 10 PASS/0 FAIL;按新约定**未发布** |
| 65 | **白板浮动置顶窗口(对标 PureRef)**——参考作画时把白板贴在绘图软件旁边:①**主进程**--`openFloatingBoard(boardId)` 创建无边框置顶小窗(680×520 可调,`frame:false` + `alwaysOnTop('floating')` + `skipTaskbar`,sandbox+contextIsolation 同主窗),dev/prod 分别以 query `?floating=1&board=<id>` 加载,已存在则聚焦复用;IPC `window:floatingOpen/Close`,主窗口关闭时联动关闭浮动窗;②**渲染层**--`main.tsx` 按 query 分流渲染 `FloatingBoard.tsx`(新建):标题条 `-webkit-app-region: drag` 整条可拖拽移动窗口(控件区 no-drag),白板名+缩放滑块+关闭按钮,主体复用 `BoardCanvas` 全量交互(缩放/平移/框选/拖动/透明度/参考线零成本继承);③**多窗口同步**--`BoardCanvas` 加 window focus 监听,窗口重新聚焦时刷新 boardItems+boards(浮动窗与主窗互相看到对方改动,避免做实时广播);④**入口**--BoardPanel 工具栏加「浮动置顶」pin 按钮;⑤新建 `.ui-shot/itest-board-floating.cjs` 专项测试(双 target CDP:主窗点按钮→浮动窗独立 target 出现→渲染断言→浮动窗内拖动持久化→主窗 DB 一致→关闭消失),实测 7 PASS/0 FAIL;回归 itest-board-multi 10 PASS、itest-board-opacity-guides 8 PASS;按新约定**未发布** |
| 66 | **白板文件 .lumenboard 导入导出(对标 MOTZ .motzboard)**——跨设备交换参考白板:①**格式**--ZIP(store 无压缩,复用 exporter 的 `zipStore` 并新增 `zipRead` 解析器:支持 store(0)/deflate(8) 两种压缩,兼容其他工具重压缩,目录条目忽略)+ `manifest.json`(app/format/version 校验、白板名、参考线 guides、元素清单含 x/y/w/h/z/text/noteFont/noteColor/opacity、文件映射 assetId→{file,name,size})+ `assets/<assetId>.<ext>` 图片原图/视频故事板(视频文件大,参考场景用封面图足够,缺原图退缩略图);②**导入策略**--新建 `boardFile.ts`:图片解包到临时目录(文件名带序号前缀)→ 走真实导入管线 `importFiles`(自动去重/缩略图/主色/dHash 全量计算)→ 序号前缀映射回 manifest(去重跳过时按 原名+size 兜底)→ 还原原名 → 建白板(同名加「（导入）」后缀)+ 参考线 + 元素(保持 z 顺序/透明度/note 样式,asset 映射失败的丢弃并记日志);③**UI**--BoardPanel 工具栏加「导出/导入 .lumenboard」按钮(导出走保存对话框,导入后自动刷新并打开新白板+toast);IPC `board:exportToPath/importFromPath`(无对话框,供测试/脚本)+ `board:exportFile/importFile`(对话框版本);④**踩坑 2 个**:a)Windows Defender 实时扫描短暂锁定临时目录,`rmSync` 抛 EPERM 且 finally 抛出会把成功导入变成错误返回——改为清理失败绝不抛出(10 次×200ms 重试,残留目录在系统临时目录由系统回收);b)electron-vite dev 对 main 进程改动有时不自动重启,测试前需确认 `main.log` 的「日志系统启动」时间戳;新建 `.ui-shot/itest-board-file.cjs` 专项测试(真实素材往返:导出→源在时导入→同名后缀→透明度/note 样式/坐标/参考线还原→素材入库→清理),实测 12 PASS/0 FAIL;全量回归 4 套 37 项全过(itest-board-multi 10 / opacity-guides 8 / floating 7 / itest-ai 7 + 本测试 12 含在内);按新约定**未发布** |
| 67 | **白板独立入口 + 可关闭(修复用户反馈:入口在标签区/白板常驻挤压素材库)**——用户反馈两点:①白板入口还在侧栏标签分区,不像独立功能;②白板默认常驻右侧且无法关闭,挤压素材库界面。修复:①**viewMode 三态化**--`'off' | 'split' | 'board'`,默认 **off(纯素材库全屏)**,白板不再常驻;②**主导航「白板」入口**(全部素材/已收藏/**白板**/回收站)——独立功能入口,点击进入白板全屏,无白板时自动创建「默认白板」,激活态高亮;③**可关闭**--BoardPanel 工具栏加「退出白板」按钮(arrowLeft 图标,新增),一键回到素材库;库导航点击(全部素材/已收藏/回收站/文件夹/标签)在白板全屏时自动退出(`setView` 内 board→off);④**侧栏白板列表点击**改为进入白板全屏(此前只切 activeBoardId,面板不可见时像没反应);⑤**移除启动自动打开第一个白板**(白板按需进入,不再默认出现);分屏模式保留(白板全屏工具栏 shapes 切换 board↔split);新建 `.ui-shot/itest-board-nav.cjs` 专项测试 9 PASS/0 FAIL(默认素材库全屏✓ 主导航入口✓ 进入✓ 退出按钮✓ 库导航自动退出✓ 分屏并存✓ 侧栏列表进入✓);同步更新 multi/opacity/floating 三个测试的前置(重载后先点「白板」进入白板模式),全量回归 53 项断言全过;按新约定**未发布** |
| 68 | **正式图标确定并接入(traced-raven 乌鸦矢量)**——用户确认采用 traced-raven 为正式图标:①**图标母版**--`build/raven-icon-source.svg`(从乌鸦照片矢量描边,含抬起的爪子,浅灰渐变天空底 + 深色乌鸦剪影,512×512 圆角方形构图);②**生成管线**--`scripts/make-icons.cjs` 从母版生成全部尺寸 PNG(16-256)+ 多尺寸 PNG 帧打包为 Windows ICO(`build/icon.ico`)+ 浏览器扩展图标(`browser-extension/icons/icon16/48/128.png`),electron-builder `win.icon` 指向 icon.ico,安装包/快捷方式/任务栏/窗口图标全部使用正式图标;③**验证**--重新运行生成脚本幂等(与仓库一致,无差异),应用内品牌为文字 Logo(glitch 风格 LUMEN)无需改渲染层;④扩展图标同步更新,需在浏览器重新加载扩展生效;随 v0.8.4 发布 |
| 69 | **白板升级为完整标注工具(对标 Excalidraw/MOTZ,用户要求往大型独立功能方向做)**——①**绘图工具**--工具栏新增第二行工具组(选择/手绘/箭头/直线/矩形/椭圆/文字,图标化):形状为矢量 SVG 元素,`board_items` 新增 `shape` 列(JSON:`{kind, points(元素内归一化 0-1), color, sw}`,ensureColumns 增量迁移),绘制实时预览/Esc 取消,支持选中/拖动/8 向缩放/框选组操作/Delete,右键菜单可改描边颜色与线宽(多选批量),样式记忆为新绘制默认;文字工具落点即建便签;②**撤销/重做**--Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y + 工具栏按钮,按白板快照 diff 恢复(删除多余/新增缺失/更新差异字段,addBoardItem 后显式还原 z 层级),覆盖绘制/拖动/缩放/删除/排列/样式/透明度/复制粘贴/置顶/文字编辑,切换白板清空历史;③**复制粘贴**--Ctrl+C/V/D + 右键「复制/粘贴」,完整保留类型/文本/字体/颜色/透明度/形状规格,粘贴偏移 +24;④**视图控制**--适配全部内容(工具栏按钮/F 键/双击空白,空画布复位 100%)、0 复位、± 缩放(画布中心锚点)、方向键微调(Shift=10px,DOM 实时 + keyup 批量落库)、缩放滑块与画布滚轮双向同步(修复滑块不跟随滚轮缩放);⑤**画布外观**--背景色(深/中灰/浅灰/白/黑 + 自定义色)+ 点阵网格开关/密度(16/24/40),按白板持久化(`boards.appearance` 列,`board:setAppearance` IPC),网格颜色随背景亮度自适应;⑥**修复 2 个 Bug**--a)note 文字便签此前完全无法拖动(textarea stopPropagation 阻断),新增顶部拖动手柄;b)**白板全屏模式画布高度为 0 的历史布局 Bug**(BoardPanel 根节点缺 h-full,全屏只剩工具栏、画布不可见,split 模式正常)——由新测试实测 frame 高度发现,加 `h-full` 修复;⑦**.lumenboard 格式兼容形状元素**(manifest 透传 shape,导入重建,实测往返还原);⑧**导出画布 SVG 矢量图**(背景/参考线/图片 dataURL 内嵌/文字 foreignObject/形状矢量,`board:exportSvg` 保存对话框);空态提示补充快捷键说明;新建 `.ui-shot/itest-board-tools.cjs` 专项测试 23 PASS/0 FAIL(工具行/手绘/矩形/箭头/撤销重做/复制粘贴/Ctrl+D/方向键微调/±与滑块同步/F 适配/0 复位/双击适配/外观切换与持久化/note 拖动手柄/.lumenboard 形状往返);全量回归 5 套 46 项断言全过(multi 10 / opacity-guides 8 / floating 7 / board-file 12 / nav 9);按新约定**未发布** |
| 70 | **白板全屏隐藏素材详情面板(用户反馈:白板下「选择一个素材查看详情」没作用)**——`App.tsx` 中 `<Inspector />` 原为无条件渲染,白板全屏时右侧 256px 详情栏仍显示空态提示,既无用又挤占画布宽度;改为 `boardViewMode !== 'board'` 才渲染:白板全屏隐藏(库不可见、无选中项可详查),split 分屏保留(素材库在左,详情面板仍有用),退出白板自动恢复;Inspector 根节点补 `data-inspector` 便于测试断言;itest-board-nav 新增 3 项断言(全屏隐藏/退出恢复/分屏保留),实测 12 PASS/0 FAIL,回归 itest-board-tools 23 PASS/0 FAIL;按新约定**未发布** |
| 71 | **双主题系统 + 银盐鸦影标准化 + 像素故障 PX2 重构 + UI debug**——①**debug 基线**:全量 TypeScript 检查通过,真实 IPC/素材协议/设置/查询 13 PASS/0 FAIL;修复纯素材库高度链缺 `min-h-0` 导致素材墙被撑到 21656px、内部无法滚动的问题,实测可滚至 `scrollTop=21028` 且底部虚拟卡片正常渲染;修复悬停预览嵌套在动画容器内使 fixed 坐标额外偏移 220px 的问题,改用 `createPortal(document.body)` 并按素材墙边界自动左右换边/底部上移;②**主题架构**:新建 `renderer/theme.ts`,定义 `silver-gelatin`/`pixel-glitch`、默认回退、本地持久化(`lumen.theme`)、`useSyncExternalStore` 订阅、首帧前初始化与 View Transition 切换,同步 meta theme-color;③**设置页主题功能**:新增两张可即时预览的主题卡,ARIA radiogroup/checked 完整,「银盐鸦影」标记默认标准;实测银盐→像素→刷新保持→银盐往返持久化✓;④**银盐鸦影 A2.2**:正式锁为默认标准,压缩顶部为 54px、侧栏 220px/详情 252px,工具栏高频动作常驻、侧栏单一纵向滚动、搜索 AI 开关独立;⑤**像素故障 PX2**:从旧换色层重构为完整视觉系统——海军黑终端/磷光青主信号/洋红校验位,等宽排版、48px 顶栏、216px 侧栏/240px 详情、像素切角卡片/弹窗、ASSET MATRIX/TRACE PANEL/NODE CANVAS 独立文案;动效改为 180-260ms 离散 panel lock/data scan/image lock,禁止持续抖动和素材扫描线/RGB 污染;⑥**主题感知组件**:Sidebar/ArchiveHeader/Inspector/BoardCanvas 随主题切换品牌铭牌、状态词与空态身份;⑦**风格资产与文档**:银盐风格卡升 A2.2 locked,新增像素 PX2 风格卡,两主题各生成首页/详情/设置/白板 4 张真实截图,截图脚本支持主题参数;项目策划案补主题产品规则、实现边界与回归闸门;⑧**最终验证**:主题/比例/滚动/悬停/持久化 12 PASS/0 FAIL,白板入口/退出/分屏 12 PASS/0 FAIL,基础 IPC/素材协议 13 PASS/0 FAIL,typecheck✓、生产 build✓、git diff whitespace✓;按新约定**未发布** |
| 72 | **v0.8.5 顶栏工具热修与辨识度优化**——①用户反馈除星标外工具看似失效,根因:双主题给 `.archive-filterbar` 加了带 `transform` 的入场动画,同时顶栏为 `overflow:hidden`;格式/颜色/日期/排序的 `position:fixed` 弹层以及重复检测/删除确认模态框仍嵌套在顶栏 DOM 内,因此 fixed 被顶栏建立的新包含块接管并裁剪,控件状态实际变化但弹层不可见;修复:Toolbar 引入 `createPortal`,所有 Popover、DupeModal 与 ConfirmDialog 统一挂载到 `document.body`;弹层 y 坐标从按钮底边升级为 `max(按钮底边+8px,整条顶栏底边+6px)`,避免与顶栏重叠;②重复素材检查不再复用普通 copy 图标,新增 `duplicateSearch` 专用符号(叠放素材+放大镜),尺寸 13→17px、标题明确为「重复素材检查」;③搜索框通用 `.field-input` padding 覆盖 Tailwind `pl-8`,导致文字与放大镜重叠,改为搜索专用左留白 36px(图标右侧实测 13px 空隙),普通/AI 搜索共用;④新增 `.ui-shot/itest-toolbar.cjs`,两主题逐项真实点击并验证弹层根层/边界/状态恢复、搜索留白和查重图标,共 38 PASS/0 FAIL;typecheck✓、生产 build✓、diff whitespace✓;测试版已启动,按新约定**未发布** |
| 73 | **素材右键菜单与二级菜单四向防越界**——用户反馈靠右素材的「添加到文件夹」二级菜单容易跑出画面;根因:一级 fixed 菜单仍嵌在带 transform/overflow 的素材墙,且二级菜单写死 `left-full top-0`,完全不判断视口空间;修复:①一级菜单与透明关闭层通过 `createPortal` 挂到 `document.body`,用 `useLayoutEffect + offsetWidth/offsetHeight` 按真实未变换尺寸钳制四边,并为入场动画预留 8px 位移安全量;②二级菜单在 hover 时读取触发行真实坐标,比较左右剩余空间自动 `left/right`,比较底部空间自动 `up/down`,箭头随方向改为 `◂/▸`;③文件夹过多时限制为 `calc(100vh - 16px)` 并内部滚动;④右键路径中的恢复原图确认框、导出对话框同步根层挂载;⑤修复连续重载时素材墙偶发停在等待 ResizeObserver 首次回调的空布局,挂载时先同步测量再监听;新增 `.ui-shot/itest-context-menu.cjs`,两主题实测右下(向左+向上)/左上(向右+向下)/一级与二级完整边界 16 PASS/0 FAIL;顶栏回归 38 PASS/0 FAIL,typecheck✓、生产 build✓、diff whitespace✓;测试版已启动,按新约定**未发布** |
| 74 | **macOS 安装包支持(GitHub Actions 双架构 CI,方案 A)**——用户需要 macOS 安装使用:①**electron-builder.yml 加 `mac` 段**--dmg + zip 双目标(自动更新用 zip),`artifactName: Lumen-${version}-${arch}`(x64/arm64 不冲突),分类 `public.app-category.graphics-design`,icon 指向 icon.icns;②**图标管线**--`make-icons.cjs` 尺寸扩到 512/1024,`icon.png` 改 512 底图,新增 macOS `iconutil` 生成 `icon.icns`(iconset 全尺寸 16→1024,仅 darwin 执行),Windows ICO 保持 ≤256 帧(ICO 规范上限);③**package.json 新增 `build:mac`**--`node scripts/make-icons.cjs && typecheck && electron-vite build && electron-builder --mac`,可追加 `--x64/--arm64 --publish never`;④**新增 `.github/workflows/build-mac.yml`**--tag `v*` 推送自动触发 + 手动 dispatch,matrix 双架构(macos-13=x64 / macos-14=arm64),`npm ci`(postinstall 自动重编译 better-sqlite3)→ `build:mac --arch` → dmg/zip/blockmap 上传到 `shiguang-materials` 同版本 Release(`CSC_IDENTITY_AUTO_DISCOVERY=false` 跳过签名;需源码仓库配置 `RELEASE_PAT` secret 指向安装包仓库,缺失时产物保留在 Actions Artifacts);新增 **`scripts/merge-latest-mac.cjs`** 合并双架构 `latest-mac.yml`(electron-builder 每架构生成一份,直接覆盖会丢另一个架构;无 YAML 依赖按行解析,根字段取 arm64,实测 BOM/缩进兼容);⑤**release.cjs 支持 `--platform mac`**--在 Mac 本地可一键打包发布当前架构(产物清单/历史清理按平台区分,win 流程零改动);⑥**README 新增「macOS 安装」章节**--下载 dmg(Intel x64 / Apple Silicon arm64)、Gatekeeper 放行三种方式(右键打开/系统设置「仍要打开」/`xattr -cr`)、zip 差分自动更新说明;⑦验证:make-icons 幂等重生成✓、merge 脚本单测通过✓、release.cjs win/mac dry-run 命令正确✓;**实机全链路验证(v0.8.8 首次双平台发布)**:a)x64 因 macos-13 排队 1h+ 改用 **macos-14(arm64) 交叉编译**方案——electron-builder `--x64` 交叉构建 + 打包前把 `node_modules/ffmpeg-static/ffmpeg` 换成 darwin-x64 二进制(ffmpeg-static 安装脚本按安装机架构下载,`binary-release-tag=b6.1.1`,换后 `chmod +x`),实测 x64 2m06s / arm64 2m38s 全绿;b)**RELEASE_PAT 必须 classic token**:fine-grained token 上传 Release 资产报 403 "Resource not accessible by personal access token"(uploads.github.com 限制),classic 勾 `repo`(或 `public_repo`)后上传成功;c)Release 资产齐 12 件(win exe/blockmap/latest.yml + mac 双架构 dmg/zip/blockmap + 合并 latest-mac.yml 含 4 条目),electron-updater 6.x `filterFilesForArch` 按机器架构挑 zip(arm64 优先/ x64 排除 arm64),自动更新链路验证通过;d)经验:SSH 推送被网络掐断时 tag 不会上传导致 mac 构建不触发(本次 v0.8.8 tag 即因此缺失,补推后自动补跑),可用 gh token 走 HTTPS 一次性推送兜底;随 v0.8.8 发布 |
| 75 | **白板工作区真正独立化 + 双侧栏专注收纳 + 外观内联抽屉**——针对用户反馈“白板里点文件夹会回到素材库、画布外观浮层遮住画布、左右区域挤占画布”:①**状态职责解耦**--`libraryStore.setView` 不再隐式把 `boardViewMode=board` 改为 `off`,文件夹/标签/收藏仅决定参考素材来源,退出白板只允许显式「退出白板」;②**专用参考素材架**--新增 `BoardReferencePanel`,白板全屏进入 BOARD/LIGHT TABLE(像素主题为 NODE CANVAS)专属界面,参考架显示当前文件夹/标签与数量、独立搜索、双列缩略图、拖放提示、单张添加/已放置状态,可直接以专用 MIME 拖入右侧画布;③**画布素材独立缓存**--BoardCanvas 按 boardItems 补取缺失 Asset,切换文件夹或把参考搜索清空后,已放上画布的图片/视频仍显示且可导出,不再依赖当前 Gallery 查询结果;④**双侧栏收纳**--素材库列表与参考素材架均可独立压成 42px/38px 窄轨,收起状态写入 localStorage;双轨同时收起实测画布宽度银盐 914.5→1318.5px、像素 918.5→1318.5px;⑤**画布外观下滑抽屉**--取消 absolute menu 与全屏关闭层,改为工具栏下方 max-height/opacity 内联展开,真实推动画布 top 84.7→142.7px且抽屉 bottom 与 frame top 对齐,不遮挡/不越界;⑥**画布平移跳图修复**--根因一是素材 pointerdown 抢走“空格+左键”导致平移被当成元素拖动,二是平移过程只写 DOM transform 而未在 pointerup 同步 React viewport state,下一次渲染会回滚视口;现在空格手势优先交给画布,中途按空格会还原未落库的元素拖动并转入平移,结束时固化 viewport 且抑制尾随 click;双主题实测从素材上平移 140×90px 后画布 transform 保持,所有素材数据库 x/y 完全不变;⑦**双主题视觉**--银盐使用 REFERENCE TRAY/底片式抽屉,像素使用节点参考架/切角卡片与离散入场,均生成真实工作区截图;⑧验证:`itest-board-workspace` 28 PASS/0 FAIL(双主题进入/文件夹不退出/拖放落库/素材上平移不改坐标/筛选独立/双轨收纳/画布扩宽/外观下推),`itest-board-nav` 13 PASS/0 FAIL,`itest-board-tools` 23 PASS/0 FAIL,typecheck✓;测试版已启动,按新约定**未发布** |
| 76 | **白板文字对象重构(对齐 Miro/tldraw 成熟交互)**——针对“不能选字号/颜色、写完仍有外框、文字工具连续生成”的反馈:①**编辑态/成品态分离**--文字不再永久渲染为 textarea;成品为透明无底无框文本,仅选中时显示选择框,双击进入带虚线编辑边界的 textarea,Ctrl/Cmd+Enter 或失焦提交,空白文字退出时自动删除;输入内容超过当前高度时文字对象自动向下长高,避免长文本被截断;②**单次放置状态机**--文字工具点击画布只创建一次,立即聚焦输入并自动回到选择工具,再次点击画布不会继续生成空文字框;用文字工具点击既有文字可直接进入编辑;③**上下文排版栏**--文字工具激活或单选文字时在画布顶部显示字体/字号/颜色,字号提供 12/16/20/28/36/48 六档,右键菜单同步补字号;样式既作用当前选中文字也记忆为下一次新建默认;④**数据闭环**--`board_items` 增 `note_font_size`(默认16,旧库 ensureColumns 增量迁移),shared/preload/IPC/repository/撤销快照/复制粘贴/SVG 导出/`.lumenboard` 导入导出全链路透传;⑤**验证**--`itest-board-tools` 升级为 30 项真实交互断言,覆盖自动聚焦/回选、无底色成品、禁止连续生成、属性栏、字号颜色持久化、双击重编、拖动与文件往返;白板工作区 28 PASS/0 FAIL、导航 13 PASS/0 FAIL、typecheck✓、生产 build✓;测试版已启动,按新约定**未发布** |
| 77 | **白板双侧窄轨主题化重绘(修复收起态像压扁侧栏)**——根据用户截图重做素材库列表/参考素材架两条收起轨:①移除四至五字竖排中文与孤立数量,改为统一信息骨架(圆形/切角展开控制、轨道编号、旋转短标签、信号节点、帧数槽、底部状态码),保留中文无障碍名称与完整 tooltip;②**银盐鸦影**使用 46px 暗房接触印样轨,`ARCHIVE INDEX`/`CONTACT SHEET`、01/02 片号、暗红曝光线、暖灰帧计数和胶片端点,弱化大段空黑;③**像素故障**使用 48px 节点总线轨,`LIB NODE`/`REF NODE`、L-01/R-02、青色分段链路、洋红状态位与四位计数,切角展开键和 16px 电路栅格形成独立语言;④悬停只增强轨道信号与展开键,不做持续故障抖动;⑤两主题真实收起截图已验收,工作区测试 28 PASS/0 FAIL,双轨收起后画布宽银盐 1314.6px/像素 1310.6px;测试版已启动,按新约定**未发布** |
| 78 | **内置完整使用说明 + 设置中心信息架构升级**——①新增 `UserGuide.tsx`,内置 9 章离线教程(第一次使用/导入与素材库/整理分类/查找预览编辑/AI/白板/导出备份/主题快捷键更新/常见问题),每章包含工作流步骤、功能边界、风险提示和快捷键,覆盖从建库到交付与排障的完整闭环;②设置弹窗升级为 1040×自适应高度的“设置与帮助”中心,左侧区分偏好设置/使用说明,偏好表单保持独立滚动,教程使用二级章节目录+正文阅读区,中等窗口内完整不越界;③全文搜索预建静态索引,支持多词 AND 匹配章节标题、关键词、正文、提示和快捷键,使用 `useDeferredValue` 避免输入阻塞,提供无结果空态和一键恢复;④双主题深度适配:银盐鸦影为暗房控制页/纸面手册,像素故障为信号配置/节点手册,两者共享信息结构但拥有独立轮廓、字体与状态信号;⑤长正文块使用 `content-visibility:auto`,无网络/数据库请求;⑥新增 `.ui-shot/itest-settings-guide.cjs`,双主题验证弹窗边界、偏好滚动、9 章目录、白板细节、搜索命中/空态/恢复、正文滚动、主题变体和关闭行为,实测 24 PASS/0 FAIL;typecheck✓;测试版已启动,按新约定**未发布** |
| 79 | **全量 debug:两路静态审查(主进程+渲染层)+ 14 套 CDP 测试全跑,修复 P0×2 / P1×7 / P2×20**--背景:里程碑 63-78 由另一批会话快速堆叠,需要系统性质量收口。①**测试修复**:3 个白板测试(floating/multi/opacity-guides)因里程碑 71 侧栏 DOM 加滚动容器后选择器过时而崩溃,统一改用 `button[aria-label="白板"]`;`itest-board-tools` 新增手绘 V 形回笔回归断言(包围盒宽/高>80 且归一化点全部在 [0,1]);②**P0 修复**:a).lumenboard 导入 Zip Slip 路径穿越(条目名 `assets/a/../../evil` 可写任意路径)--白名单化单段安全文件名+非法字符清洗;b)手绘笔包围盒只取首尾点,V/S/圈等回笔笔画 w/h≈0 整体被裁剪丢失--预览与提交两处都改为遍历全部采样点求 min/max;③**P1 修复**:a)zipRead 补 CRC32 校验+数据越界报错(损坏 ZIP 此前静默返回截断数据入库);b)白板视频素材 `<video src=静态四宫格>` 无法解码显示空白--统一走 `<img>`(mkv/avi 等此前靠巧合兜底);c)Delete 批量删除 fire-and-forget 竞态(刷新先于删除返回,已删元素闪回)--`Promise.all` 后再刷新;d)拖动/绘制/平移/参考线拖动均无中断兜底--window blur 统一回滚所有进行中手势 + frame/元素/手柄补 onPointerCancel;e)浮动窗复用时新 boardId 被丢弃(用户以为浮动的是 B 实际还是 A)--抽出 `floatingBoard.ts` 模块,复用窗 `webContents.send('board:switch')` 切换白板,FloatingBoard 监听热切;f)永久删除素材不清理白板引用(幽灵空框/导出静默丢元素)--deleteAssets 级联删 board_items;g)store 模块加载时裸读 localStorage(隐私模式整窗白屏)--try/catch 回退;④**P2 修复**:空格 keyup 丢失卡死平移(blur 重置)、撤销历史被无意义快照污染(无位移点击/已置顶点击/拖动尾随 click 三处掐掉)、window 键盘监听无焦点区域判断(split 模式劫持图库 Ctrl+A/方向键,App.tsx 同步让位)——frame tabIndex=-1 交互即聚焦+editable 含 select/contenteditable、Esc 在输入框内不再清选中、切板补 setEditingNoteId(null)、zoomTo 改画布中心锚点(与滚轮/±一致)、onImgLoad 补 naturalHeight>0、画布素材缓存单条 getAsset 容错、框选补指针捕获可跨边界、绘制中途切工具取消笔画、undo/redo/粘贴共用 busy 位串行化、SVG 导出 noteColor 白名单+noteFont 过滤(防恶意 .lumenboard 注入)且空缩略图跳过(防 fetch('') 抓页面)、滚轮缩放与拖动手势互斥(中途变焦落库坐标错位)、排列不再改写 note/shape 尺寸且高度口径统一 aspectCache、背景色板抽 `APPEARANCE_PRESETS` 导出共用(两处色值漂移)、.lumenboard 往返补 appearance(背景/网格)、临时目录清理 Atomics.wait 同步阻塞(最坏 2s 冻结主进程)改异步、导入/导出损坏文件补错误 toast、撤销/重做按钮禁用态(onHistoryChange)、updateBoardItems 的 updated_at 并入事务、bringBoardItemToFront z 起点统一 -1、VIDEO_EXTS 抽 shared 三处共用、BoardItemPatch 类型收窄;⑤**验证**:typecheck✓,白板专项 7 套 + 全量 14 套 CDP 测试全绿;按新约定**未发布** |
| 80 | **遗留低优问题收口(白板体验细节)**--里程碑 79 审查报告里剩余的 P2/P3 逐项清理:①**参考线**--a)key 改用稳定 id(新建时生成,旧数据按 位置+方向 回退),删除中间线不再卸载重挂;b)拖动改 DOM 直改(此前每帧 setGuides 重渲染整棵画布,大画布卡顿),松手统一走 `finishGuideDrag` 落定持久化,blur 兜底同步;②**点阵背景**--平移逐帧只改 surface 与 grid 两处 DOM(此前点阵用滞后 state,松手才跳到位),新增 `applyGridTransform` 取模同步;③**外部添加可撤销**--参考架「+」/素材库批量发送走 `sendAssetsToBoard` 的记录添加前快照到 `boardHistoryHint`,BoardCanvas 消费入撤销栈,与画布内操作行为一致(Ctrl+Z 可撤);④**参考架已放置判定竞态**--store 新增 `boardItemsBoardId`(元素清单归属),切白板立即清空元素+标记未知,加载完成前禁用添加按钮,不再出现"刚切板时重复入板";⑤**focus 刷新互斥**--多窗口同步刷新在 本地拖动/绘制/平移/框选/微调/参考线拖动/文字编辑 进行中跳过,防异步回写与 DOM 中间态交错;⑥**组包围盒**--移除 JSX 里读 `dragRef.current` 的条件渲染(反模式,靠 display:none 兜底),组缩放进行中不再因 ref 变化导致包围盒闪现;⑦**ZIP 健壮性**--zipStore 加 ZIP64 守卫(条目>65535/单条目>4GB/文件名>65535 明确报错而非静默截断),zipRead 的 EOCD 倒查加中央目录自洽校验(注释内假签名不再误判),manifest.version>1 导入明确报错;⑧**死通道清理**--移除未暴露的 `asset:paths`/`trash:cleanOld` 死注册;⑨**测试**--itest-board-workspace 新增「参考架添加可撤销(Ctrl+Z 移除)」断言,itest-board-tools 手绘 V 形断言保留;全量 14 套 237 项断言全绿,typecheck✓;按新约定**未发布** |
| 81 | **更新弹窗长日志越界修复 + 全量同类弹窗审计**--用户反馈 Release Notes 过长时把“重启安装”挤出画面,只能关闭弹窗。根因是通用 `ConfirmDialog` 让标题、任意长度正文和操作按钮共同参与自然高度,没有视口上限与独立正文滚动。修复为固定标题/可滚动正文/固定操作栏三段式结构,宽高同时受视口安全区约束,“取消/重启安装”始终可见;下载前“发现新版本”卡片同步加视口上限与说明区收缩滚动。审计全部非全屏弹窗后额外加固两个同类风险点:智能文件夹(条件/标签可无限增长)与导出素材(小高度窗口),均改为只滚动内容、保存/导出固定在底部;AI 处理、重复检查、标签合并/管理、设置中心原有高度链与独立滚动结构确认安全。新增 `.ui-shot/itest-update-dialog.cjs`,以 80 条长日志在银盐鸦影/像素故障两主题验证下载前卡片和下载后确认框的边界、三区布局、正文滚动与滚动后按钮位置,14 PASS/0 FAIL;主题/工具栏/素材滚动链回归 12 PASS/0 FAIL;typecheck✓、diff whitespace✓;按新约定**未发布** |
| 82 | **后端优化阶段 1:主进程导入管线全量异步化 + 导入进度推送(消除同步 IO 冻结)**--按《后端优化方案》里程碑 81/阶段 1 执行(编号因并行会话占用 81 顺延为 82):①**importer.ts 同步 IO 全量异步化**--`collectFiles` 改 `fs/promises` 异步递归(`readdir withFileTypes`,不存在路径 try/catch 跳过保持原语义,调用方 `importFiles`/`watcher.syncOnStartup` 补 await);`prepareOne` 的 `statSync/mkdirSync/copyFileSync/rmSync(move 删源)/writeFileSync(缩略图两处)` 全改 `stat/mkdir/copyFile/rm/writeFile`;`psdToRaw` 的 `readFileSync` 整读 PSD 改 `await readFile`;`rmSafe` 的 `Atomics.wait(150ms)` 同步睡眠改 `await new Promise(setTimeout)`(最坏 750ms 主进程冻结消除,Defender 锁重试语义不变);`commitBatch` 改 async:metadata.json 写盘移出 `db.transaction` 闭包(其与 DB 原子性无关,单文件失败仍仅告警不回滚,`now` 时间戳口径不变),事务只包 SQL;查重逻辑(isDuplicate 四路判定)/两阶段结构(阶段 A 并发计算+阶段 B 事务提交)/并发度(`cpus().length`)均未动;②**导入进度推送**--`ImportOptions` 增可选 `onProgress(phase:'prepare'|'commit',done,total)`,阶段 A 在 `mapWithConcurrency` 每文件完成时回调、阶段 B 提交后补一次 commit 事件;`ipc.ts` 的 `import:dialog`/`import:paths` 与 `main/index.ts` 的 `import:fileObjects` 三入口统一接 `getWindow()?.webContents.send('import:progress',{phase,done,total})`;preload 增 `onImportProgress`(照抄 onAiProgress 模式)与 `importFromPaths`(既有 `import:paths` 通道的包装,主进程零改动);App.tsx 照抄 aiProgress 卡片模式新增导入进度卡(「导入素材中…/导入完成」+done/total+进度条,commit 后 2s 自动隐藏);③**clipServer/backup 写盘异步化**--clipServer `saveClip` 的 `writeFileSync` 改 `await writeFile`;backup `backupLibraryToZip` 最终写盘改 `await writeFile`(函数转 async,ipc `library:backupZip` 补 await;entries 攒批读取属阶段 3 流式化范围未动);④**新增 `.ui-shot/itest-import-async.cjs`(9 断言)**--渲染层 canvas 生成 5 张 1600x1200 PNG 写入临时目录 -> `importFromPaths` 真实导入:断言 imported=5 且 0 失败;导入期间并发 20 次 `getAppVersion` 全部正常返回且最大延迟<500ms(实测 1ms,证明主进程不再被同步 IO 阻塞);收到 6 次 `import:progress`(5 prepare+1 commit,轮询 3s 等终态 commit--实测事件经 IPC 派发可能晚于 invoke 返回,直接读会竞态漏采);5 条记录落库尺寸 1600x1200;结束软删除测试素材+清理临时目录;⑤**验证**--typecheck(node+web)通过;全量 16 套 CDP 测试全绿(15 旧+1 新);按新约定**未发布** |
| 83 | **后端优化阶段 2a:数据库索引 + 语句缓存(后端优化方案任务 2.1/2.2)**--①**补三个热路径索引**(db.ts migrate 幂等追加):`idx_assets_hash_size`(导入查重哈希路径此前每次全表扫,EXPLAIN 实测 SCAN->SEARCH)、`idx_assets_name_size`(查重快速路径)、`idx_assets_deleted`(libraryStats 的 deleted 计数实测命中覆盖索引不回表;注意回收站分页查询优化器会合理选 idx_assets_imported 免排序,不是缺陷);②**语句缓存**(新建 `src/main/stmtCache.ts`):按 SQL 文本缓存 better-sqlite3 statement,消除热路径每次调用重新 prepare 的编译开销;改造 isDuplicate(导入查重,每文件 1-2 次)、deleteAssets(prepare 移出 for 循环)、assetPaths(asset: 协议每张缩略图请求都走,最热语句)、queryAssets/getAssetById/restoreAssets/emptyTrash/listTags/setAssetTags/addToFolder/白板全套语句;低频管理路径按方案不动;**生命周期双保险**:closeDb() 先 clearStmtCache() 再关连接(切库必清),stmt() 内部再校验 `s.database !== db` 不一致即重编译,任何未走 closeDb 的换库路径也安全;③**新增 `scripts/bench-db.cjs`**:ELECTRON_RUN_AS_NODE 兜底 ABI 直接开库跑 EXPLAIN QUERY PLAN,--assert 断言三条查询命中新索引,实测 3 PASS(过程中修复脚本自身两个断言 bug:COVERING INDEX 变体不匹配/回收站断言选错查询形状);④**新增 `.ui-shot/itest-db-stmt.cjs` 切库回归(6 PASS)**:library:remove 移除唯一库触发真实 closeDb->重开全周期(纯现有 IPC 无对话框),断言重开前后 stats/listTags/listBoards 一致不抛错、重开后 INSERT/DELETE 语句可用;⑤typecheck✓,全量 17 套 CDP 测试全绿(16 旧+1 新);执行说明:本里程碑由子 agent 起草(因配额中断),协调者验收补完(bench 断言修正/切库回归测试/回归验证);按约定**未发布** |
| 84 | **后端优化阶段 2b:颜色数量物化列(任务 2.3;2.4 核实后取消)**--①**color_count 物化列**:db.ts ensureColumns 增列(默认 0)+ 启动迁移全量回填 `UPDATE assets SET color_count = json_array_length(colors)`(自愈幂等,兜底任何写入遗漏);写入路径同步维护:importer commitBatch 的 INSERT 与 editor applyEdit/revertEdit 的 UPDATE 各加 color_count(colors.length);查询侧 queryAssets 的 colorCountMax 从逐行 `json_array_length(colors)` 函数计算改 `color_count <= ?` 列比较(函数计算无法利用索引);②**任务 2.4(backupDatabase 改 VACUUM INTO)核实后取消**:原方案判断有误,backup.ts 实际用的是 better-sqlite3 `db.backup()` 在线热备 API(SQLite Backup API),本身即 WAL 一致性快照并非裸拷贝,现状保持,方案文档已修正;③bench-db.cjs 新增物化列断言:与 json_array_length 不一致行数=0 + 两种口径计数等价,实测 4 PASS;④验证:typecheck✓,字节级扫描无模板污染,全量 17 套 CDP 测试全绿(itest-new 的 colorCountMax 断言自动走新列路径);按约定**未发布** |
| 85 | **后端优化阶段 3:共享 zipLib + 流式 ZIP(内存优化)**--①**新建 `src/main/zipLib.ts`** 统一纯 JS ZIP 实现(原 exporter.ts 与 backup.ts 各持一份复制,共 200+ 行重复):crc32(含流式增量版)/zipStore(ZIP64 守卫)/zipRead(CRC 校验+EOCD 自洽,以 exporter 版为准)/**zipStoreStream** 流式写入器--store 模式两遍法(第一遍流式算 CRC/字节数,第二遍写本地头+流式复制),本地头写真实 CRC/size,输出与内存式**逐字节一致**,系统解压工具全兼容;`for await` 原生背压(逐块等待排空,不整文件进内存);drain 监听用完即移除(修掉 MaxListeners 告警);②**exporter.ts** 删除本地 ZIP 实现改从 zipLib 导入,exportToZip 转流式+async(原图走 filePath 过流,内存只剩 metadata 清单),ipc 调用点补 await;③**backup.ts** 删除重复实现,backupLibraryToZip 全流式化:异步目录遍历(fs/promises)+ 打包前 `wal_checkpoint(TRUNCATE)` 保证 library.db 完整(busy 时连同 -wal 打包 SQLite 也能恢复)+ 逐文件流式写入;④**boardFile.ts** 改从 zipLib 导入(.lumenboard 通道不变);⑤**新增 `scripts/bench-backup.cjs`**:esbuild 现场转译 zipLib.ts 验证真实源码--混合大小条目(含 20MB 跨块文件+中文名)新旧实现**逐字节一致**(21509630 字节)、zipRead 回读 CRC 全过、**真库 1682 文件 827.9MB 流式备份堆增长 -1.4MB**(旧实现需 ~1.6-2.5GB 内存);⑥验证:typecheck✓,全量 17 套 CDP 测试全绿(含 .lumenboard 往返 12 断言与备份路径);按约定**未发布** |
| 86 | **后端优化阶段 4:repository/ipc 按领域拆分(纯重构,行为零变化)**--①**repository 拆分**:`src/main/repository.ts`(941 行)拆为 `repository/` 目录五个域文件--assets.ts(查询/查重/相似/删除恢复/哈希,479 行)、tags.ts、folders.ts、stats.ts、boards.ts(白板全套),`index.ts` 聚合 re-export,全部 `from './repository'` 既有导入零改动;拆分时同步处理跨域依赖(folders 的 countByConditions 依赖 assets 的 queryAssets,stats 的 cleanTrashOlderThan 依赖 deleteAssets,均改为显式 './assets' 导入,无循环);修复两个拆分事故(段边界切断 updateBoardAppearance 尾部,从 git 恢复;相对路径 ./db -> ../db 批量修正);②**ipc 拆分**:`ipc.ts`(485 行 74 个 handler)拆为 `ipc/` 九个域文件(library/import/assets/settings/ai/tags/folders/boards/system),各自 `registerXxxIpc(getWindow)`,`index.ts` 聚合 `registerIpc` 并 re-export `resolveAssetFile`(asset: 协议用),`src/main/index.ts` 导入零改动;③**验证**:typecheck✓,全量 17 套 CDP 测试全绿(74 个通道逐项真实调用验证注册无缺漏/无重复);纯移动无任何逻辑改动(可用 git diff -w 复核);按约定**未发布** |
| 87 | **后端优化阶段 5:健壮性收尾(watcher 批量合并/413 状态码/DB 自愈)**--①**watcher 批量合并**(watcher.ts 重写):文件事件攒进 Set(天然去重同文件多事件),500ms 防抖窗口统一 flush 成一次 importFiles 批量导入(此前每文件独立定时器+独立导入);loadConfig 每次 flush 一次(此前每事件);stopWatchers 清定时器与池;②**clipServer 413**(修复真实 Bug):body 超限先回 `json(413)` 再断连(此前直接 destroy 客户端只见连接重置),且 catch 里 `headersSent` 守卫(此前 413 后 catch 又写 400 导致 ERR_HTTP_HEADERS_SENT 未处理异常);③**DB 打开完整性检查+自愈**(db.ts):openDb 跑 `PRAGMA quick_check`,失败走 recoverCorruptDb--损坏库改名 `.corrupt-<ts>` 保留现场、清 -wal/-shm(防重放损坏日志)、有 .bak 则恢复并复检、无备份建空库并 dialog 明确告知(数据丢失必知);migrate 移入独立 try,失败=代码问题,关闭清空单例重抛(不误走损坏自愈路径,修复测试暴露的坏实例残留);④**新增测试**:`.ui-shot/itest-stage5.cjs`(CDP+node 直连:413 状态码/400 路径/watcher 批量 3 图仅 1 次通知/配置恢复)、`scripts/test-db-recovery.cjs`(esbuild 转译真实 db.ts:损坏+备份恢复/无备份重建/正常库不误伤 6 PASS);⑤**排障实录**(本阶段耗时大头):a)测试误报"watcher 不工作"——实为历史测试垃圾(损坏假 PNG)已入库被查重正确 skip,且库中损坏素材让素材墙 404 连发+findDuplicates 补哈希反复失败拖垮应用(itest.cjs 图片加载断言被牵连);b)我的手动实验 dev 未杀干净占用 9333/45678 端口,污染后续整批测试(与并行会话实例叠加);c)实验脚本误把用户 watchDirs 清空(已恢复);清理 9 个损坏测试素材+恢复配置后**全量 18 套测试全绿**;按约定**未发布** |
| 88 | **退出白板按钮改版(用户反馈:白板右上角退出按钮不起眼)**--原为 24×24 纯图标按钮(arrowLeft 图标 + 暗色文字),混在一排同尺寸图标按钮里难以发现;改版为**图标+文字的描边动作按钮**:①新 CSS 类 `board-exit-btn`(index.css 白板样式区):26px 高、12px 字号、`--border-strong` 描边、"退出白板"文字标签 + arrowLeft 图标,hover 转警示色(边框/文字/底色三重反馈);②双主题适配:银盐鸦影沿用圆角暗房风;像素故障加切角 + 硬阴影 + 数据字体,hover 洋红告警位;③aria-label/title 保持不变(3 处测试选择器零改动);④CDP 双主题实测:按钮 94×26(银盐)/96×26(像素),文字标签渲染✓ 描边✓ 切角生效✓,itest-board-nav 13 PASS + itest-board-workspace 30 PASS(退出流程/双主题回归)✓,typecheck✓;按约定**未发布** |
| 89 | **白板放大模糊修复:原图/缩略图按缩放切换(方案 B,对标 PureRef 像素级真实)**--用户反馈白板放大图片发虚:根因是白板渲染一直用导入时的 512px 缩略图(assetThumbUrl),CSS 放大即浏览器插值虚化,而 PureRef 始终加载原始分辨率像素(缩小锐利/放大是像素感而非模糊)。实现:①**滞回缩放切换**--新增 ORIG_ZOOM_UP=1.25 / ORIG_ZOOM_DOWN=0.85 阈值,画布 scale 放大超阈值开启原图、缩回阈值以下切回缩略图(滞回防抖动,缩回控大图显存);②**视口内才加载**--按当前 viewport 计算画布可见矩形,仅与可见区域相交的静态图元素才叠加原图(大板不一次性拉全量原图);③**缩略图打底 + 原图淡入覆盖**--原图叠加层 opacity 0→1(150ms 淡入),未加载完成不闪空白;④**浏览器可解码白名单**--BOARD_ORIG_EXTS(jpg/png/gif/webp/bmp/avif/tiff/svg),heic/psd/ai 等无浏览器原图仍用缩略图;视频仍走故事板(无现成原图帧);原图 URL 带 &e=edited 戳防缓存;⑤**测试**--itest-board-workspace 新增双主题断言:显式归位 scale=1 → 按 + 到 1.25 → 视口内元素出现 data-board-orig 且淡入完成(银盐 1 张/像素 2 张)→ 按 - 到 ≤0.85 → 原图层移除;实测双主题 PASS;全量 18 套测试全绿,typecheck✓;按约定**未发布** |
| 90 | **白板支持外部文件拖入(对标 PureRef:Explorer 拖图直接上板)**--用户反馈白板模式下从外部拖图片进来没效果:根因是画布拖放只处理内部素材 MIME(application/x-eaglelike-assets),系统文件(types=Files)的 drop 被放过并冒泡到 App 层只导入素材库,不会出现在画布上。修复:①**拖放处理器扩展**(BoardCanvas):onDragOver 接受 Files 类型,onDrop 分支处理 Files--`importFileObjects`(既有通道,主进程 webUtils 转真实路径)→ 导入后取新素材 id → `refreshAssets` + `addAssetsToBoard(ids, 落点)` 放到鼠标位置 + toast「已导入 N 张并加入白板」;重复文件(skipped>0 无新导入)提示可从参考架拖入;stopPropagation 防 App 层二次导入;②**ImportResult 增加 `importedIds`**(shared/importer 两处):importFiles 阶段 B 成功后回填本次成功导入的 id 列表(供白板放置新素材,向后兼容新增字段);③**测试**(itest-board-workspace 新增双主题断言):真实临时 PNG 走 importFromPaths 返回 importedIds 长度 1 ✓、合成 File 拖放(webUtils 无法解析路径)优雅空操作不崩溃不重复入板 ✓,测试后软删清理;全量 18 套测试全绿,typecheck✓;说明:真实 OS 拖放(File->真实路径)无法在 CDP 合成事件模拟,主链路由 importedIds+既有 MIME 拖入(同样走 addAssetsToBoard)共同覆盖;按约定**未发布** |
| 91 | **debug 发现并修复:拖拽导入真实 Bug(主进程 webUtils 不存在)**--用户要求发布前全量 debug。全量测试全绿时,主进程日志里一行 `拖拽文件路径解析失败: Cannot read properties of undefined (reading 'getPathForFile')` 暴露真相:`webUtils` 仅在 preload/渲染进程可用,**主进程为 undefined**,而里程碑 82 的 `import:fileObjects` 处理器把它写在主进程——意味着**从 82 起所有拖拽导入文件(图库拖入、白板外部拖入)实际从未真正导入过**,只是测试用合成 File 恰好走到"优雅空操作"路径掩盖了故障。修复:①File→路径的转换改到 **preload**(webUtils 可用),`importFileObjects` 改为 preload 内转路径后走既有 `import:paths`(返回含 importedIds),新增 `getFilePaths(File[])` 暴露;②主进程删除损坏的 `import:fileObjects` 处理器与 webUtils/importFiles 未用导入;③**新增 `.ui-shot/itest-drop-path.cjs` 真实链路 E2E**:用 CDP `DOM.setFileInputFiles` 把真实磁盘文件注入 input(唯一能让渲染进程持有带真实路径 File 的手段,模拟 OS 拖放),断言 getFilePaths 解析出真实路径 ✓、importFileObjects 真实导入成功并返回 importedIds ✓、合成 File 优雅返回空数组 ✓(3 PASS);④回归:全量 19 套验证,唯一失败为 itest-theme 的 hover 预览时序 flake(独立重跑 12/12 全过,与本次改动无关);typecheck✓、污染扫描 0、无残留进程;按约定**未发布** |
| 92 | **自动备份 + 保留策略(优先级优化 #1)+ 项目策划案更新至里程碑 91(#8)**--①**自动备份**(backup.ts 扩展 + index.ts 调度):启动延迟 20s 后自动执行(不拖慢启动),数据库备份每日至多一次、全量 ZIP 每周至多一次;备份存 userData/backups(避免把自己包进全量递归);**保留策略**:db 轮转保留 3 代(library.db.bak/.1/.2),ZIP 保留最近 2 份自动清理;marker(.last-auto.json)记录 lastDb/ISO 周 lastZip,**每步成功后增量写**(全量 zip 耗时中进程被杀也不重复已完成步骤);仅打包版默认启用,开发模式 LUMEN_AUTO_BACKUP=1 强制(供测试)、LUMEN_AUTO_BACKUP_DELAY 覆盖延迟;②**验证**:新增 scripts/test-backup-rotation.cjs 纯逻辑 4 PASS(轮转 3 代/最新为 v5/超出删除/ZIP 保留 2 份);强制 env 起 dev 实测:db.bak 3 代生成 ✓ 全量 974MB zip ✓ marker lastDb+lastZip 写入 ✓(清理验证产物);③**#8 策划案更新**:已完成区补里程碑 82-92(后端工程化六阶段/原图切换/外部拖入/退出按钮/自动备份),进行中/规划中表刷新(CI 冒烟/孤儿清理/拼音搜索/查询列收窄/FTS5 为规划项),发布节奏里程碑数 81→92;④回归:全量 19 套中仅 board-floating 合成拖拽 flake 一次(独立重跑 7/7 全过,与本次改动无关);typecheck✓;按约定**未发布** |
| 93 | **CI 冒烟测试(优先级优化 #2)**:让"每次 push 自动回归"替代"想起来才跑"。①**种子测试库**(scripts/ci-seed-library.cjs + test-fixtures/sample.png):esbuild 转译 db.ts 在全新目录生成接近真实形态的最小库(12 图/1 非空文件夹「胶片」/1 标签组+标签/2 张内容相同图用于查重/1 条回收站软删),并写指向它的 userData config.json;应用通过 HOME(CI)/APPDATA(本地模拟)指向种子库即可在干净环境跑测试;②**runner 排除支持**(scripts/itest.cjs):ITEST_EXCLUDE 环境变量按文件名子串跳过(CI 排 itest-ai 需 AI key、itest-update-dialog 需网络/Release);③**`.github/workflows/smoke.yml`**:ubuntu 上 push/PR 自动 npm ci → typecheck → 种子库 → xvfb 无头跑全量 CDP(ELECTRON_DISABLE_SANDBOX=1 兼容 CI 容器),失败即红;④**本地实证**(Windows 用 APPDATA 指向种子库模拟 CI):排除上述 2 个后 **17 套全绿 EXIT=0**——证明种子库方法可行、排除集正确;⑤排障:种子脚本 bundle 需落在项目内(否则 better-sqlite3 解析不到)、重复图分支文件名笔误;按约定**未发布** |
| 94 | **启动维护任务(#3 孤儿清理 + #5 哈希回填;优先级优化 #4 评估后延后)**--新建 `src/main/maintenance.ts`:①**孤儿素材目录清理**:导入是"先复制文件、后写库",中途崩溃会留下无 DB 记录的目录(数据卫生);启动时扫描 assets/ 下匹配 严格结构 `assets/xx/{16hex}` 且无 DB 记录的目录,保守删除并告警;非资产结构(任意文件/不符 id 形态)一律不动;②**dHash 启动回填**:老库/异常导入的素材 hash='',此前 findDuplicates/findSimilar 每次查询都反复现算,改为启动时对 hash='' 的图片一次性回填(并发池,与导入同款);③**调度**:index.ts 在自动备份后 +3s 延迟执行(异步不阻塞启动);函数支持注入 db/libPath(测试用临时库,生产默认全局库);④**新增 scripts/test-maintenance.cjs**(esbuild 转译真实源码):孤儿 1 个被清理/有效目录保留/非资产结构保留/dHash 回填 2 张且内容相同则哈希一致,5 PASS;⑤**#4 查询列收窄评估后延后**:Asset 单一类型被列表/详情/AI 范围共用,exif/comment 等大字段 Inspector 也要用,收窄需拆分类型改渲染层多处,在 500-5k 规模收益低,记入「进行中/规划」(素材破万再连同 FTS5 一起做);⑥验证:typecheck✓,全量 19 套 CDP 全绿;按约定**未发布** |
| 95 | **安装目录选择(NSIS assisted 安装器)**--用户提议:安装器应允许选安装目录(对标 Eagle;素材库是重度空间应用,用户常想装 D 盘)。现状:electron-builder 默认 oneClick 一键安装,装到 %LOCALAPPDATA%\Programs\拾光素材库,无目录选择。改动(electron-builder.yml nsis 段):`oneClick: false` + `allowToChangeInstallationDirectory: true`(向导式可选目录),**关键权衡**:`perMachine: false` 保持每用户安装(默认目录不变,用户可改到 D:\xxx 等每用户可写目录;不鼓励 Program Files,否则自动更新需提权断链);electron-updater 对 assisted + per-user 自定义目录是标准支持,老用户更新链不受影响。**验证**:发布 v0.8.15 后对安装器做静默自定义目录实测——经 cmd 绕开 Git Bash 的 MSYS 参数转换(/S、/D= 会被改写)后,`setup.exe /S /D=C:\...\LumenCustomInstall` 成功完整装到自定义目录(含 ShiGuangMaterials.exe + 卸载器),卸载清理;另修复 CI 冒烟第二轮失败:①种子库步骤普通 node 下 db.ts 自愈路径对不存在文件 rename 抛 ENOENT(修:文件缺失跳过 rename 直接重建);②seed 改 ELECTRON_RUN_AS_NODE 执行(与本地一致)+ 幂等;③`ws` 非声明依赖,本地靠传递依赖侥幸可用而 npm ci 没有(修:加 devDependency);随 v0.8.15 发布 |
| 96 | **CI 冒烟最终收敛:无头 Linux 稳定性四轮修复后全绿**--背景:里程碑 93 建的 smoke.yml 在 GitHub Actions 无头 xvfb 环境反复失败,本地 17 套全绿而 CI 上崩,根因集中为"无头 Linux 上重载/导入/sharp 场景 Electron 直接崩溃"与"userData 路径语义不同"。修复序列:①**XDG_CONFIG_HOME 语义**(0577ce8)--Linux 下 userData 由 XDG_CONFIG_HOME 决定而**不走 $HOME env**,workflow 此前设 HOME 指向种子库完全无效,改为显式设 `XDG_CONFIG_HOME` 指到种子库目录;②**收敛为稳定子集**(c144a6d)--无头 Linux 上重导入类测试(import 系/itest-drop-path/itest-stage5 等 16 套)实测易崩,冒烟收敛为不重载不导入的稳定子集,重导入类测试留本地跑;③**种子库建到默认路径**(86096f6)--itest-db-stmt 的 removeLibrary 切库后回到默认路径,种子库建到 `$HOME/EagleLike.library`(与本地模拟一致)避免"切库后带偏后续测试";④**LUMEN_HEADLESS 禁 GPU 稳化**(e857617)--index.ts 在 `LUMEN_HEADLESS=1` 时追加 `disable-gpu` + `disable-software-rasterizer`,进一步降低无头崩溃面;**最终稳定子集 = itest-db-stmt + itest-settings-guide + itest.cjs** 三项(纯 db 语句/设置中心/基础回归,均不重载不导入);**验证**:GitHub Actions 全绿--安装依赖→类型检查→生成种子测试库→CDP 冒烟回归(xvfb 无头)全部 success,CI 冒烟正式可用;随 v0.8.16 发布 |
| 97 | **白板超大规模性能优化(策划案「进行中」项):视口裁剪 + 渲染降级 + 元素级 memo**--背景:BoardCanvas 此前 `boardItems.map` 无条件渲染全部元素,千元素白板 = 千 DOM 节点 + 千张缩略图,且滚轮缩放/视口变化每次全量重渲染,平移缩放随规模线性变卡。改动(全渲染层,零主进程/IPC 改动):①**视口裁剪**--frame 两阶段测量(`useLayoutEffect` 量帧后二次渲染,paint 前完成无闪烁;首帧布局前 `cullRect=null` 先渲染空 surface),`cullRect` = 可见矩形外扩一个视口余量(短平移不露空白),只渲染与裁剪矩形相交的元素,正在编辑的文字豁免(卸载即丢编辑内容);②**平移期间 rAF 节流同步裁剪**--平移全程 DOM 直改(不逐帧 React 渲染),裁剪矩形靠 rAF 每帧至多一次 `setViewport` 同步(长距平移新划入元素即时挂载、原图叠加层跟手),rAF 不调度(CDP/后台窗)时 pointerup 的 applyViewport 兜底;窗口 resize 监听强制重算裁剪;③**小尺寸渲染降级**--asset 元素屏幕渲染尺寸 < 22px 时用主色色块占位(`data-degraded`,读 colors[0]),免除缩小视图下数千张缩略图的解码/绘制(最大瓶颈),文字/形状保持可读不降级;④**元素级 React.memo**--提取 `BoardItemView`(memo + 浅比较),事件回调走 `itemApiRef`(每次渲染刷新为最新闭包、ref 身份稳定),视口/选中等父级变化时未变更元素直接跳过渲染;原图叠加层判定(elig)下推到父级算成 `origSrc` 字符串,元素输出与视口解耦(仅阈值/视口边缘元素重渲染);⑤**选中判定 O(1) 化**--`selectedSetRef`(Set)替代数组 `includes`,`isSelected`/`selectionBBox` 全选大板时从 O(n²) 降 O(n);⑥**新增 .ui-shot/itest-board-perf.cjs**(15 断言):80 元素 400px 间距宽幅网格下 DOM 渲染数 20/80(裁剪生效)、空格平移 -3400/-2800 后 12/80 且 80 项坐标零改写(裁剪跟随)、缩放 10% 时 asset 降级色块/恢复 100% img 回归、Ctrl+A 全选 81 元素组包围盒正常;踩坑:测试 add 循环误用内层 `(async()=>{})()` 包裹导致 80 次 add 在后台跑、切板刷新提前触发只加载 3 条(实现本身正确,改顶层 await 修复);验证:typecheck✓,全量 CDP 测试全绿(19 旧 + 1 新);按约定**未发布** |
| 98 | **拼音/模糊搜索(对标 Eagle,策划案规划项)**--背景:库里 532 个中文名素材只能输入完整中文名检索,拼音/首字母搜不到。改动:①**引入 pinyin-pro**(纯 JS 无原生模块,多音字支持:重庆→chongqing),新建 `src/main/pinyin.ts` 包装 `computeNamePinyin`--含 CJK 的名字生成全拼小写串联(`人物造型概念参考`→renwuzaoxinggainiancankao)与首字母串(rwzxgnck,去标点数字);纯 ASCII 名返回空串(检索侧沿用 name LIKE,不生成冗余拼音);②**assets 表加 name_pinyin/name_pinyin_init 两列**(ensureColumns 增量迁移);③**三条写入/回填路径**:导入 commitBatch INSERT 写入、updateAsset 改名同步重算(AI 改名/白板文件恢复原名自动覆盖)、启动维护任务 `backfillMissingPinyin` 兜底存量(name_pinyin='' 且含 CJK 的行一次性回填,之后恒为 '' 的只有纯 ASCII 名);④**检索扩展**(queryAssets 与 searchAssets 同口径):关键词含字母时额外匹配 `name_pinyin LIKE ? OR name_pinyin_init LIKE ?`(先 `name_pinyin != ''` 短路;关键词去空格/连字符后再 LIKE,输入「rw zx」也能命中),中文关键词不加拼音分支避免无效 LIKE;⑤**测试**:新增 `scripts/test-pinyin.cjs`(9 断言:esbuild 转译真实源码,拼音正确性/多音字/ASCII 空串/混合名/回填逻辑;better-sqlite3 按 Electron ABI 编译,系统 node 直接跑报 NODE_MODULE_VERSION 不匹配,照 bench-db.cjs 模式用内存库探针提前触发校验并自动 ELECTRON_RUN_AS_NODE 重跑自身)+ `.ui-shot/itest-pinyin.cjs`(11 断言:导入中文名素材后首字母/全拼/混合/中文关键词检索命中、无关拼音不误命中、改英文名拼音失效/改回中文重算恢复);⑥**实测**:启动回填真实库 532/729 行(62ms),中文素材全部可拼音检索;typecheck✓,全量 CDP 测试全绿(20 旧 + 1 新);按约定**未发布** |
