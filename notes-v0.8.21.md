# LUMEN v0.8.21

本版修复更换推理型 AI 模型（如 DeepSeek）后的处理失败问题，并将 Windows 安装向导名称统一为 LUMEN。

## 🐛 修复
· 推理型模型（deepseek-v4-flash 等）AI 处理批量失败：思考链与正文共享 max_tokens 预算，旧上限被思考吃光导致返回为空或 JSON 截断
· AI 打标 / 搜索 / 视觉精排 / 测试连接四处调用点统一调大 token 预算并放宽超时，对任何推理型模型通用

## ⚙️ 优化
· Windows 安装向导标题统一显示 LUMEN（原「拾光素材库」；快捷方式与卸载项此前已同步），安装目录与升级链路不受影响
