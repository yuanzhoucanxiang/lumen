# LUMEN 后端(主进程)优化方案

> 执行者须知:本文档自包含,不依赖任何会话上下文。行号以 v0.8.10(commit a2b3bed)为基准,后续会漂移,**以函数名/代码片段定位为准**。按阶段顺序执行,每阶段独立提交一个里程碑,验收通过再进下一阶段。
>
> **发布约定(重要)**:所有改动只提交到本地 git,**不要发布**。只有用户明确说"汇总新版本/推送更新"时才运行 `node scripts/release.cjs`。

## 一、背景与目标

后端 = `src/main/`(20 个文件约 4600 行)。整体底子良好:SQL 全参数化、事务正确、AI 有超时并发池、日志有轮转、导入两阶段设计。本方案解决五个结构性问题:

| # | 问题 | 用户感知 |
|---|------|---------|
| 1 | 主进程同步文件 IO,导入大文件时冻结全部 IPC/UI | 导入时界面卡死 |
| 2 | 数据库缺索引 + 语句每次重编译 + 颜色筛选全量拉取 | 大库查询/查重变慢 |
| 3 | ZIP 备份/导出全量进内存(存 2-3 份拷贝) | 大库备份内存爆炸 |
| 4 | ipc.ts 74 个 handler / repository.ts 928 行单文件 | 维护困难 |
| 5 | WAL 下裸拷贝 db 备份可能丢数据等健壮性小项 | 数据安全 |

**总原则**:行为不变(通道名、数据格式、UI 交互全部不动),只提升响应性/性能/内存/组织度。渲染层改动仅限阶段 1 的进度条消费,其余阶段零渲染层改动。

## 二、现状证据(已核实)

### 2.1 同步 IO 阻塞主进程
`src/main/importer.ts`(604 行):
- L1:导入 `copyFileSync, mkdirSync, readdirSync, readFileSync, rmSync, statSync, writeFileSync`(全同步)
- L55-57 `collectFiles`:`statSync`/`readdirSync` 同步递归遍历
- L122-136 `rmSafe`:重试间隔用 `Atomics.wait(...150ms)` **同步睡眠**,最坏阻塞主进程 750ms
- L228 `psdToRaw`:`readFileSync` 整读 PSD(可能几百 MB)
- L393 `prepareOne` 内 `statSync`
- L431-432:`copyFileSync` 复制原图(200MB 视频 = 冻结数秒)+ move 时 `rmSync`
- L461/L504/L561:`writeFileSync` 写缩略图/故事板/metadata.json
- sharp 本身在 libuv 线程池执行(异步),**阻塞源是文件 IO**,不是图像计算
- `importFiles`(L570)无任何进度事件;对比:AI 处理已有 `ai:progress` 推送(见 ipc.ts L251)

其他:`src/main/clipServer.ts` L4 `writeFileSync`;`src/main/backup.ts` L1 同步 IO。

### 2.2 数据库层
`src/main/db.ts`:
- L9 已开 WAL ✅;已有索引:imported/name/ext/star/width_height、asset_tags(tag_id)、asset_folders(folder_id)、deleted_files 两个、board_items(board_id)
- **缺失**:`assets(hash, size)`——查重哈希路径每次全表扫(tombstone 表反而有索引,本末倒置);`assets(name, size)`——查重快速路径;`assets(deleted_at)`——所有查询都过滤它

`src/main/importer.ts` L72-114 `isDuplicate`:**每次调用重新 `prepare`**(导入时每文件调 1-2 次)
`src/main/repository.ts`:
- L289-315 `deleteAssets`:`db.prepare` 写在 for 循环**内部**,每个 id 重编译 3-4 条语句
- L758 附近 `addBoardItem`:每次调用重新 prepare
- L91-195 `queryAssets`:颜色路径 `limit 20000`,`rowToAsset` 对每行 `JSON.parse(colors)`,再 `matchColor` 内存过滤(2 万次 parse)
- L166-169:颜色数量筛选已下推 `json_array_length(colors)`,但仍是逐行函数计算,无法用索引

### 2.3 ZIP 内存问题
- `src/main/backup.ts` L11-85:与 `src/main/exporter.ts` L31-110 **重复实现** crc32/zipStore(两份拷贝)
- `backup.ts` L113 `backupLibraryToZip`:遍历库目录,把**每个文件** `readFileSync` 进 `entries` 数组,最后 `zipStore` 再 `Buffer.concat` 拼成单块——整个库在内存中存 2-3 份
- `exporter.ts` L248-280 `exportToZip`:同款模式
- `backup.ts` L87 `backupDatabase`:WAL 开启状态下直接复制 `library.db` 文件,**未 checkpoint 的写入不在主 db 文件里,备份可能是旧数据**

### 2.4 组织
- `src/main/ipc.ts`:485 行 74 个 `ipcMain.handle`,单文件(有分区注释但仍是 god file)
- `src/main/repository.ts`:928 行混 assets/tags/folders/boards/stats 五个领域

### 2.5 健壮性
- `src/main/watcher.ts` L11-33:每个文件事件独立 `importFiles([file])`,无批量合并;每次事件 `loadConfig()`
- `src/main/clipServer.ts` `readBody`:body 超 80MB 时 `reject + req.destroy()`,客户端收到连接重置而非 413
- `db.ts` `openDb`:无完整性检查,库文件损坏时直接抛异常无恢复路径

## 三、阶段任务

### 阶段 1:消除主进程阻塞(里程碑 81-82,价值最高)

**任务 1.1 importer 同步 IO 全部异步化**(里程碑 81)
- `collectFiles` 改异步递归(`fs.promises.readdir(dir, { withFileTypes: true })`),调用方 `importFiles`/`watcher` 已是 async,顺手改签名;注意 `boardFile.ts` 不用它,无涟漪
- `prepareOne`:`statSync`→`fs.promises.stat`,`mkdirSync`→`mkdir`,`copyFileSync`→`copyFile`,`writeFileSync`(缩略图/故事板/metadata.json)→`writeFile`,`rmSync`(move 删除源)→`rm`
- `psdToRaw` 的 `readFileSync`→`await readFile`
- `rmSafe` 改 async:`Atomics.wait`→`await new Promise(r => setTimeout(r, 150))`;调用处加 await
- **禁止**改动查重逻辑、两阶段结构(阶段 A 计算/阶段 B 事务提交)、并发度(`cpus().length` 保持)
- 已有 CDP 兼容坑提醒:测试用合成事件,rAF/指针捕获等有历史坑,本任务不碰渲染层

**任务 1.2 导入进度推送**(里程碑 81)
- `importFiles(paths, opts)` 增加可选 `onProgress?: (phase: 'prepare'|'commit', done: number, total: number) => void`,阶段 A 在 `mapWithConcurrency` 的 fn 完成时回调
- `ipc.ts` 的三个入口(`import:dialog`/`import:paths`/`import:fileObjects`)接入,推 `getWindow()?.webContents.send('import:progress', {...})`
- `preload/index.ts` 加 `onImportProgress` 监听(照抄 `onAiProgress` 模式)
- `App.tsx` 消费:照抄现有 `aiProgress` 卡片模式(简单复用同一卡片,标注"导入中"),**最小改动**

**任务 1.3 clipServer / backup 同步写异步化**(里程碑 81)
- `clipServer.ts` 的 `writeFileSync`→`await writeFile`(handler 已 async)
- `backup.ts` 的同步 IO 本阶段只顺手改写文件部分,大改留给阶段 3

**验收(里程碑 81)**:
- `npm run typecheck` 通过
- 全量 `npm run test:itest` 14 套全绿(含 itest.cjs 的真实导入管线断言)
- 新增 `.ui-shot/itest-import-async.cjs`:导入 5+ 张图,期间并发 `await window.api.getAppVersion()`×20 测 IPC 延迟,断言导入完成且期间每次版本号返回 < 500ms;断言收到 3+ 次 `import:progress` 事件

**任务 1.4 视频故事板生成路径核查**(里程碑 82)
- 核查 importer 中 ffmpeg `spawn` 路径(L470-510 一带)已是异步事件驱动;若发现同步等待(`execSync`/`Atomics.wait`)一并改掉;确认 storyboard 写盘已随 1.1 异步化
- 里程碑 82 空间留给 1.1/1.2 的回归修复缓冲;若全部一次做完则并入 81,82 起阶段 2

### 阶段 2:数据库层(里程碑 82-83)

**任务 2.1 补索引**(db.ts `migrate` 内追加,幂等)
```sql
CREATE INDEX IF NOT EXISTS idx_assets_hash_size ON assets(hash, size);
CREATE INDEX IF NOT EXISTS idx_assets_name_size ON assets(name, size);
CREATE INDEX IF NOT EXISTS idx_assets_deleted ON assets(deleted_at);
```
注意 `hash=''` 的非图片记录也入索引,体积可控;`deleted_at` 大部分为 NULL,索引选择性一般但对回收站查询有效。

**任务 2.2 语句缓存**(新建 `src/main/stmtCache.ts`)
```ts
import type Database from 'better-sqlite3'
const cache = new Map<string, Database.Statement>()
export function stmt(db: Database.Database, sql: string): Database.Statement {
  let s = cache.get(sql)
  if (!s) { s = db.prepare(sql); cache.set(sql, s) }
  return s
}
export function clearStmtCache(): void { cache.clear() }
```
- **关键坑**:statement 绑定在具体 Database 实例上。`closeDb()`(db.ts L19)必须调用 `clearStmtCache()`,否则切换素材库后用到旧实例的 statement 会抛错。这是本任务唯一真正的风险点,务必写 CDP 测试覆盖"设置里切库后再查询"路径(已有 listLibraries/addAndSwitchLibrary)
- 改造调用点:`importer.ts isDuplicate` 全部、`repository.ts deleteAssets`(prepare 移出循环)、`addBoardItem`、`rowToBoardItem` 等高频路径;低频路径(设置类)可不动

**任务 2.3 颜色数量物化列**
- `ensureColumns(d, 'assets', { color_count: 'INTEGER NOT NULL DEFAULT 0' })` + 迁移回填 `UPDATE assets SET color_count = json_array_length(colors) WHERE color_count = 0 AND colors != '[]'`(幂等,注意存量空 colors 行)
- 写入侧:importer `commitBatch` 的 INSERT 增加 color_count(由 colors 数组长度)
- 查询侧:`queryAssets` 的 `q.colorCountMax` 条件从 `json_array_length(colors) <= ?` 改 `color_count <= ?`
- 颜色筛选(`q.color`,内存距离匹配)保持现状不动——距离计算无法下推,且已有 20000 上限

**任务 2.4 `backupDatabase` 改一致性快照**
- 用 `stmt(db, 'VACUUM INTO ?').run(targetPath)` 替代裸拷贝(删除旧 .bak 时注意 Windows 文件锁,复用 rmSafe 思路但异步)
- VACUUM INTO 目标已存在会报错:先删再跑,失败回退旧拷贝并 logger.warn

**验收**:`EXPLAIN QUERY PLAN` 前后对比(`WHERE hash=? AND size=?` 从 SCAN 变 SEARCH idx_assets_hash_size);`itest-new.cjs` 的查重阈值/构图断言不回归;新增切库回归断言(stmtCache 坑)。

### 阶段 3:流式 ZIP(里程碑 84)

**任务 3.1 抽共享 zipLib**
- 新建 `src/main/zipLib.ts`:合并 exporter.ts 与 backup.ts 的 crc32/zipStore/zipRead(exporter 的 zipRead 已含 CRC 校验与 EOCD 自洽,以它为准),exporter/backup/boardFile 改从 zipLib 导入,**行为零变化**
- 现有 `.ui-shot/test-zip.cjs` 回归

**任务 3.2 流式 ZIP 写入器**
- zipLib 增加 `zipStoreStream(entries: { name: string; filePath: string }[] | 混合 data/filePath, out: Writable)`:逐条目流式写
- 简化设计(避免 data descriptor 兼容性风险):store 模式每个文件**两遍法**——第一遍 `createReadStream` 增量算 CRC32 与总长,第二遍流式复制到输出;本地头写真实 CRC/size(与现有 zipRead 及系统解压工具完全兼容)
- CRC32 增量版:`crc32Update(crc, chunk)`,表初始化复用
- 16MB 读写块;`backpressure` 用 `for await` 自然处理
- `backupLibraryToZip`/`exportToZip` 改用流式版:逐文件 `stat` 取 size、流式写入,不再 entries 数组攒全量
- backupLibraryToZip 打包 library.db 前:若 db 打开着,先 `wal_checkpoint(TRUNCATE)` 保证 zipped db 完整
- **验收**:新增 `scripts/bench-backup.cjs`(直接 node 跑,不走 CDP):对现有库(约 500 素材)分别用旧/新实现生成 zip,断言字节数一致或解压内容一致(process.memoryUsage().heapUsed 峰值记录对比,预期数量级下降);`itest-board-file.cjs`(12 断言)回归

### 阶段 4:代码组织(里程碑 85,纯重构)

**任务 4.1 拆 repository**
- 建 `src/main/repository/` 目录:`assets.ts`(query/search/get/update/delete/restore/stats/tombstone 相关)、`tags.ts`、`folders.ts`、`boards.ts`(全部 board_*)、`stats.ts`(libraryStats 等)
- `src/main/repository/index.ts` 原样 re-export 全部函数——**所有 `from './repository'` 的导入零改动**

**任务 4.2 拆 ipc**
- 建 `src/main/ipc/` 目录:按现有分区注释切 `assets.ts`/`tags.ts`/`folders.ts`/`boards.ts`/`ai.ts`/`system.ts`(设置/备份/剪藏/更新/窗口/导出),`index.ts` 聚合导出 `registerIpc`
- `src/main/index.ts` 的 `import { registerIpc } from './ipc'` 保持不变(目录 index 兜住)
- 删除旧单文件前跑全量测试;**通道名、参数、返回值一个都不许变**

**验收**:全量 14 套 CDP + typecheck + `git diff` 确认无行为改动(纯移动)。

### 阶段 5:健壮性收尾(里程碑 86)

**任务 5.1 watcher 批量合并**
- 事件攒进 `Set<string>`,500ms 窗口(沿用现有防抖值)统一 flush 成一次 `importFiles([...paths])`;`loadConfig` 每次 flush 调一次(不再每事件)
- 保持 `checkTombstone: true` 语义

**任务 5.2 clipServer 正确状态码**
- `readBody` 超限时:先 `json(res, 413, { ok: false, error: 'body too large' })` 再 `req.destroy()`(注意先回包再断);handler 签名传 res

**任务 5.3 DB 打开完整性检查与恢复**
- `openDb` 后跑 `PRAGMA quick_check`(只取第一行结果);失败时:
  1. `logger.error` 记录
  2. 把损坏库改名为 `library.db.corrupt-<ts>`
  3. 若存在 `library.db.bak`(backupDatabase 产物)则复制为主库并重开;无 bak 则重新建空库
  4. 通过现有 toast 链路通知渲染层(或 `dialog.showErrorBox`,执行者按简洁取舍)
- quick_check 每次启动跑一次(库大时 <1s,可接受;若实测慢改 `PRAGMA quick_check(1)`)

## 四、明确不做(避免执行者跑偏)

- **不换异步 SQL 驱动**(better-sqlite3 同步模型是桌面单用户正确选择)
- **不上 worker_threads/utilityProcess 跑导入管线**(sharp 已在 libuv 线程池;fs 异步化后主线程空闲;PSD 纯 JS 解码暂无真实卡顿案例)
- **不上 FTS5**(当前库规模 LIKE 足够)
- **不改任何 IPC 通道名/参数/返回结构**(阶段 4 是物理拆分不是接口变更)
- **不动渲染层交互**(除 1.2 的进度卡片)
- **不发布**(见顶部约定)

## 五、工程约定(执行者必读)

1. **测试**:`npm run test:itest`(全量 14 套,自动起 dev + CDP 9333);单跑 `npm run test:itest -- --only board`。新增测试放 `.ui-shot/itest-*.cjs`(照抄现有结构:ws 连 9333 → Runtime.evaluate 断言)
2. **typecheck**:`npm run typecheck`(node+web 两个 tsconfig)
3. **提交**:每里程碑一个 commit,中文 message 前缀 `perf:`/`refactor:`/`fix:`,同步在 `工作日志.md` 里程碑表追加一行(格式照抄 | 86 | 风格)
4. **已知环境坑**:
   - electron-vite dev 对 main 进程改动偶发不热重启,测试前看 `%APPDATA%/LUMEN/logs/main.log` 的启动时间戳
   - Git Bash 显示中文乱码是显示层问题,文件本身 UTF-8 无损,用 python 读写验证
   - git push 被网络掐时用 `git push "https://x-access-token:$(gh auth token)@github.com/yuanzhoucanxiang/lumen.git" main`(`Authorization` header 形式会被凭证管理器拦截)
   - Windows Defender 会短暂锁新建文件,删临时目录要重试(现有 rmSafe 模式)
5. **禁止发布**,见顶部。

## 六、里程碑与顺序

| 里程碑 | 内容 | 依赖 |
|--------|------|------|
| 81 | 阶段 1(1.1-1.3)+ 新增导入异步/进度测试 | 无 |
| 82 | 阶段 2(2.1/2.2 索引+语句缓存)+ 切库回归 | 81 |
| 83 | 阶段 2(2.3/2.4 颜色物化列 + VACUUM INTO) | 82 |
| 84 | 阶段 3(zipLib 合并 + 流式写入 + bench) | 无(与 82/83 无耦合,可并行) |
| 85 | 阶段 4(拆 repository/ipc,纯重构) | 83/84 合并后做,避免拆分期间冲突 |
| 86 | 阶段 5(watcher 批量/413/DB 自愈) | 无硬依赖,收尾 |

每里程碑交付物 = 代码 + 测试全绿 + typecheck + 工作日志行 + 本地 commit。
