# LUMEN 项目策划案

> 面向 CG 与视觉创作者的本地素材库 + 创作参考白板
> 更新日期:2026-08-15 · 对应里程碑 1-74

---

## 一、项目定位

**LUMEN(光的单位,呼应"拾光")**——一个把"素材库"和"创作参考白板"合二为一的本地桌面应用:

- **素材库**:像 Eagle 一样收图、分类、检索、管理(标签/文件夹/智能文件夹/AI 能力)
- **白板**:像 PureRef / MOTZ 一样把一批参考图自由摊开、比较、排布,边画边参考

目标用户:概念设计师、插画师、原画师、CG 创作者——"平时把素材收进库,做项目时摊开在白板上反复参考"。

## 二、核心功能规划

### ✅ 已完成(里程碑 1-73)

**素材管理**
- 导入(拖拽/对话框/粘贴/监控文件夹/浏览器剪藏),复制/移动双模式,启动增量同步
- 多级文件夹树、智能文件夹(条件组合)、标签体系(分组/颜色/优先/排除)
- 搜索(关键词/标签/颜色/构图/尺寸/日期)、dHash 查重与相似图搜索
- EXIF 元数据、视频故事板封面、HEIC/PSD/字体预览、非破坏性编辑、批量导出(ZIP/命名模板/按标签分文件夹)
- 回收站(软删/恢复/永久删/自动清理)、数据库备份与完整库导出

**AI 能力(智谱 GLM-4V,OpenAI 兼容)**
- AI 智能改名 + 自动打标签(标签库复用/同义词归并/分类归组)
- 用户优先标签与排除标签
- AI 自然语言搜索(语义扩展 → SQL 候选 → 视觉精排)

**创作参考白板(对标 PureRef / MOTZ / Excalidraw)**
- 无限画布:滚轮缩放(光标锚点)/ 空格或中键平移/点阵网格(开关/密度)
- 素材发送到白板(卡片 send-chip/批量发送/拖拽,3 列错开摆放)
- 元素:图片/视频/文字 note/矢量形状;拖动/8 向手柄缩放/置顶/删除
- 标注工具:手绘笔/箭头/直线/矩形/椭圆(实时预览,颜色/线宽可调并记忆)
- 撤销/重做(Ctrl+Z/Y,覆盖绘制/移动/缩放/删除/样式/粘贴)
- 复制粘贴(Ctrl+C/V/D + 右键菜单,保留全部样式)
- 框选多选(组移动/组缩放/组删除/Ctrl+A,对标 MOTZ group-selection)
- 元素透明度调节(预设/滑块,多选批量,对标 PureRef 参考图对比)
- 可拖动参考线(添加/删除/持久化)
- 画布外观:背景色切换(深浅/白/黑/自定义)+ 网格开关密度(按白板持久化)
- 视图控制:适配全部内容(按钮/双击/F)/0 复位/± 缩放/方向键微调/缩放滑块双向同步
- 白板浮动置顶窗口(无边框小窗常驻顶层,复用画布全量交互)
- .lumenboard 白板文件导入导出(图片内嵌,跨设备交换,对标 .motzboard)
- 导出画布为 SVG 矢量图(图片内嵌 base64)
- 自动排列(网格/横向/纵向)、note 字体与颜色
- split 分屏(素材库+白板同屏,可拖宽)+ 全屏切换

**工程与体验**
- 自动更新(electron-updater,超时重试)、快捷键自定义、Markdown 备注
- 更新已就绪持久角标、日志导出、集成测试(CDP)
- 主题系统:设置页即时切换与本地持久化,首次启动默认「银盐鸦影」;「像素故障」作为第二套完整主题,两者分别拥有独立字体、比例、组件轮廓、状态文案与动效语法
- 视觉基线:正式乌鸦图标、银盐鸦影 A2.2 默认标准、像素故障 PX2 校准版;首页/详情/设置/白板均有真实截图验收产物
- macOS 双架构安装包:GitHub Actions 自动构建 x64/arm64 的 dmg+zip,随 Windows 包同版本发布与自动更新(未签名,首次需 Gatekeeper 放行)

### 🔄 进行中

- 白板标注工具进阶:图层管理/对齐吸附/网格吸附、更多画笔(高亮/橡皮擦)
- 白板撤销重做纳入素材发送(send-chip)等 store 侧操作

### 📋 规划中

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 代码签名 | 消除 Windows SmartScreen 警告与 macOS Gatekeeper 拦截(需购买证书 + Apple Developer 账号) | 中 |
| 多语言 | 界面 i18n(中/英) | 中 |
| 插件/脚本 | 导出到 Photoshop 等外部工具的桥接 | 低 |
| 云端同步 | 素材库多设备同步(需自建服务,谨慎评估) | 低 |

## 三、主要参考项目

### 1. Eagle(素材管理标杆)
- **官网**: https://eagle.cool
- **借鉴点**:
  - 标签 + 文件夹 + 智能文件夹的三层组织方式
  - 浏览器剪藏扩展(右键/整页/区域截图)
  - 监控文件夹自动导入、启动增量同步
  - 自动更新链路(GitHub Releases + electron-updater)
  - 素材信息面板(标签/颜色/EXIF/备注)
- **我们不做的**:付费闭源模式;Eagle 的插件生态依赖其专有格式,我们保持 SQLite 开放可迁移

### 2. PureRef(白板标杆)
- **官网**: https://www.pureref.com
- **借鉴点**:
  - 极简克制:一个画布 + 一个小工具栏,界面几乎不打扰
  - 图片自由摆放/缩放/透明度调整,反复对比参考
  - 置顶悬浮窗口(参考时贴在绘图软件旁边)
  - 参考图排列(网格/横向/纵向)、画布背景切换
- **我们不做的**:无素材库管理(这正是我们比 PureRef 强的地方);付费闭源

### 3. MOTZ 白板(一体化参考)
- **GitHub**: https://github.com/motzifystudio/motz-whiteboard
- **借鉴点**:
  - 素材库与白板 split 分屏同屏工作(素材卡片 send-chip 一键发送)
  - 8 向缩放手柄、画布右键菜单、note 字体/颜色面板
  - 发送位置自动错开算法(3 列网格 + 宽高比定尺寸)
  - 白板文件 .motzboard 打包交换(图片内嵌,跨设备打开)
- **我们不做的**:纯 JSON 文件存储(我们用 SQLite 更稳);无 AI 能力

### 其他参考
- **billfish / 千鹿设计助手**:国内同类素材管理,学习其交互细节
- **Pinterest**:视觉灵感采集的标签/板子模型(远期可借鉴"灵感板"概念)

## 四、技术架构

| 层 | 技术 |
|----|------|
| 桌面框架 | Electron 41(三进程:main/preload/renderer,contextIsolation + sandbox) |
| 界面 | React 19 + TypeScript 5.9 + Tailwind CSS 4 + zustand 5;`data-theme` 令牌与组件变体 + View Transition 即时切换 |
| 数据 | SQLite(better-sqlite3,事务/递归 CTE/WAL)+ 原图按 id 分片存储 |
| 图片处理 | sharp(libvips 8.17,支持 HEIF/AVIF/PSD 合成)+ ag-psd |
| 视频 | ffmpeg-static(封面/故事板/时长) |
| AI | OpenAI 兼容格式(智谱 GLM-4V 默认,可切通义/Ollama/OpenAI) |
| 发布 | electron-vite 3 + electron-builder + GitHub Releases 自动更新 |

## 五、发布节奏

- **里程碑制**:每个功能/修复 = 一个里程碑,记录于 `工作日志.md`(当前 73 个)
- **版本**:累积一批里程碑后汇总发布(`scripts/release.cjs` 一键:版本 bump → notes → 打包 → Release → tag)
- **仓库**:源码 `yuanzhoucanxiang/lumen` · 安装包 `yuanzhoucanxiang/shiguang-materials`

## 六、风险与决策记录

| 事项 | 决策 | 原因 |
|------|------|------|
| 白板对标 | PureRef 极简理念 + MOTZ 交互实现 | PureRef 的克制 + MOTZ 的素材库一体化 |
| 数据格式 | SQLite 而非 JSON | 查询/索引/事务能力,标签体系依赖 |
| AI 供应商 | 默认智谱,OpenAI 兼容 | 国内可用 + 可切换 |
| 安装包 | 不签名(SmartScreen 提示) | 签名证书成本,用户可"仍要运行" |
| 发布频率 | 攒批发布 | 减少用户更新打扰 |
| 默认主题 | 银盐鸦影 A2.2 | 用户指定为默认与视觉标准;突出素材、适合长时间整理 |
| 第二主题 | 像素故障 PX2 | 保留数字终端个性,但把故障限制为短促状态反馈,禁止污染原图 |
| 主题持久化 | 渲染层 `localStorage(lumen.theme)` | 主题是设备级界面偏好,需在 React 首帧前应用,避免启动闪烁且无需进入素材库业务配置 |

## 七、主题系统与视觉标准

### 7.1 产品规则

- 设置页「主题」是完整界面语言切换,不是单纯换色。主题必须同时定义:设计令牌、字体、信息密度、栏宽/顶栏高度、卡片与弹窗轮廓、状态文案、动效和 reduced-motion 行为。
- `银盐鸦影 / RA–01` 是默认主题与金标准。首次启动、主题值缺失或非法时必须回落到该主题。
- `像素故障 / PX–02` 是可选主题。它可以更锐利和数字化,但素材原图、缩略图和预览不得被持续扫描线、RGB 色偏或噪声覆盖。
- 主题选择即时生效,写入 `lumen.theme`,刷新和重启后保持;主题色同步更新窗口 `theme-color`。
- 浮动白板与主窗口共享同一主题来源,避免同一工作流出现两套视觉语言。

### 7.2 实现边界

- 主题模块:`src/renderer/src/theme.ts`,集中维护主题 ID、默认值、元数据、持久化、订阅与切换。
- DOM 契约:`<html data-theme="silver-gelatin|pixel-glitch">`;CSS 通过主题根选择器覆盖令牌和组件语法。
- 主题敏感文案由 `useTheme()` 驱动,包括品牌铭牌、工作区抬头、状态词、详情栏和白板空态。
- 设置主题不进入 SQLite/素材库配置:切换素材库不得改变用户界面偏好。

### 7.3 回归验收

每次新增或修改主题至少验证:

1. 首页、单选详情、设置、白板四个页面截图。
2. 中等窗口下工具栏无功能挤出,侧栏与素材库可纵向滚动且无横向溢出。
3. 首列/末列/底部素材的悬停预览始终留在素材墙可视边界内。
4. 两主题往返切换、刷新持久化、非法/缺失值回落默认主题。
5. TypeScript 全量检查、生产构建、基础 IPC 集成测试。
