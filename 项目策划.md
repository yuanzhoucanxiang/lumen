# LUMEN 项目策划案

> 面向 CG 与视觉创作者的本地素材库 + 创作参考白板
> 更新日期:2026-08-14 · 对应里程碑 1-66

---

## 一、项目定位

**LUMEN(光的单位,呼应"拾光")**——一个把"素材库"和"创作参考白板"合二为一的本地桌面应用:

- **素材库**:像 Eagle 一样收图、分类、检索、管理(标签/文件夹/智能文件夹/AI 能力)
- **白板**:像 PureRef / MOTZ 一样把一批参考图自由摊开、比较、排布,边画边参考

目标用户:概念设计师、插画师、原画师、CG 创作者——"平时把素材收进库,做项目时摊开在白板上反复参考"。

## 二、核心功能规划

### ✅ 已完成(里程碑 1-62)

**素材管理**
- 导入(拖拽/对话框/粘贴/监控文件夹/浏览器剪藏),复制/移动双模式,启动增量同步
- 多级文件夹树、智能文件夹(条件组合)、标签体系(分组/颜色/优先/排除)
- 搜索(关键词/标签/颜色/构图/尺寸/日期)、dHash 查重与相似图搜索
- EXIF 元数据、视频故事板封面、HEIC/PSD/字体预览、非破坏性编辑、批量导出(ZIP/命名模板/按标签分文件夹)
- 回收站(软删/恢复/永久删/自动清理)、数据库备份与完整库导出

**AI 能力(智谱 GLM-4V,OpenAI 兼容)**
- AI 智能改名 + 自动打标签(标签库复用/同义词归并/分类归组)
- 用户优先标签与排除标签
- AI 自然语言搜索(语义扩展 → SQL 候选 → 视觉精排)

**创作参考白板(对标 PureRef / MOTZ)**
- 无限画布:滚轮缩放(光标锚点)/ 空格或中键平移/点阵背景
- 素材发送到白板(卡片 send-chip/批量发送/拖拽,3 列错开摆放)
- 元素:图片/视频/文字 note;拖动/8 向手柄缩放/置顶/删除
- 框选多选(组移动/组缩放/组删除/Ctrl+A,对标 MOTZ group-selection)
- 元素透明度调节(预设/滑块,多选批量,对标 PureRef 参考图对比)
- 可拖动参考线(添加/删除/持久化)
- 白板浮动置顶窗口(无边框小窗常驻顶层,复用画布全量交互)
- .lumenboard 白板文件导入导出(图片内嵌,跨设备交换,对标 .motzboard)
- 自动排列(网格/横向/纵向)、note 字体与颜色
- split 分屏(素材库+白板同屏,可拖宽)+ 全屏切换

**工程与体验**
- 自动更新(electron-updater,超时重试)、快捷键自定义、Markdown 备注
- 更新已就绪持久角标、日志导出、集成测试(CDP)

### 🔄 进行中

- 白板对标 PureRef 的进一步极简化(工具栏/交互细节)

### 📋 规划中

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 代码签名 | 消除 SmartScreen 警告(需购买证书) | 中 |
| 多语言 | 界面 i18n(中/英) | 中 |
| 插件/脚本 | 导出到 Photoshop 等外部工具的桥接 | 低 |
| 云端同步 | 素材库多设备同步(需自建服务,谨慎评估) | 低 |

## 三、主要参考项目

### 1. Eagle(素材管理标杆)
- **官网**: https://eagle.cool
- **借鉴点**:
  - 标签 + 文件夹 + 智能文件夹的三层组织方式
  - 浏览器剪藏扩展(右键/整页/区域截图)
  - 监控文件夹自动导入、启动增量同步
  - 自动更新链路(GitHub Releases + electron-updater)
  - 素材信息面板(标签/颜色/EXIF/备注)
- **我们不做的**:付费闭源模式;Eagle 的插件生态依赖其专有格式,我们保持 SQLite 开放可迁移

### 2. PureRef(白板标杆)
- **官网**: https://www.pureref.com
- **借鉴点**:
  - 极简克制:一个画布 + 一个小工具栏,界面几乎不打扰
  - 图片自由摆放/缩放/透明度调整,反复对比参考
  - 置顶悬浮窗口(参考时贴在绘图软件旁边)
  - 参考图排列(网格/横向/纵向)、画布背景切换
- **我们不做的**:无素材库管理(这正是我们比 PureRef 强的地方);付费闭源

### 3. MOTZ 白板(一体化参考)
- **GitHub**: https://github.com/motzifystudio/motz-whiteboard
- **借鉴点**:
  - 素材库与白板 split 分屏同屏工作(素材卡片 send-chip 一键发送)
  - 8 向缩放手柄、画布右键菜单、note 字体/颜色面板
  - 发送位置自动错开算法(3 列网格 + 宽高比定尺寸)
  - 白板文件 .motzboard 打包交换(图片内嵌,跨设备打开)
- **我们不做的**:纯 JSON 文件存储(我们用 SQLite 更稳);无 AI 能力

### 其他参考
- **billfish / 千鹿设计助手**:国内同类素材管理,学习其交互细节
- **Pinterest**:视觉灵感采集的标签/板子模型(远期可借鉴"灵感板"概念)

## 四、技术架构

| 层 | 技术 |
|----|------|
| 桌面框架 | Electron 41(三进程:main/preload/renderer,contextIsolation + sandbox) |
| 界面 | React 19 + TypeScript 5.9 + Tailwind CSS 4 + zustand 5 |
| 数据 | SQLite(better-sqlite3,事务/递归 CTE/WAL)+ 原图按 id 分片存储 |
| 图片处理 | sharp(libvips 8.17,支持 HEIF/AVIF/PSD 合成)+ ag-psd |
| 视频 | ffmpeg-static(封面/故事板/时长) |
| AI | OpenAI 兼容格式(智谱 GLM-4V 默认,可切通义/Ollama/OpenAI) |
| 发布 | electron-vite 3 + electron-builder + GitHub Releases 自动更新 |

## 五、发布节奏

- **里程碑制**:每个功能/修复 = 一个里程碑,记录于 `工作日志.md`(当前 62 个)
- **版本**:累积一批里程碑后汇总发布(`scripts/release.cjs` 一键:版本 bump → notes → 打包 → Release → tag)
- **仓库**:源码 `yuanzhoucanxiang/lumen` · 安装包 `yuanzhoucanxiang/shiguang-materials`

## 六、风险与决策记录

| 事项 | 决策 | 原因 |
|------|------|------|
| 白板对标 | PureRef 极简理念 + MOTZ 交互实现 | PureRef 的克制 + MOTZ 的素材库一体化 |
| 数据格式 | SQLite 而非 JSON | 查询/索引/事务能力,标签体系依赖 |
| AI 供应商 | 默认智谱,OpenAI 兼容 | 国内可用 + 可切换 |
| 安装包 | 不签名(SmartScreen 提示) | 签名证书成本,用户可"仍要运行" |
| 发布频率 | 攒批发布 | 减少用户更新打扰 |
