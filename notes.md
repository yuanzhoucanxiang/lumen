✨ 新功能
· AI 智能重命名 + 自动打标签：多选素材，AI 看图生成文件名和标签，直接应用（对标 Eagle）
· 支持 OpenAI 兼容格式：智谱 GLM-4V / 通义千问 VL / Ollama / OpenAI 均可接入
· 设置页配置 AI（Base URL + API Key + 模型），支持测试连接
· 批量处理进度卡片实时显示，3 并发限流，单张失败不阻断

⚙️ 优化
· 标签追加式（不覆盖已有标签），改名可撤销（Ctrl+Z）
· API Key 安全：存主进程，脱敏返回（仅末 4 位），不进渲染进程
