✨ 新功能
· AI 预览审核模式：生成建议后先展示可编辑预览（改文件名/删标签/勾选），确认后再应用
· AI 标签智能分类归组：标签按场景/风格/主体/色调自动归入 AI-场景/AI-风格/AI-主体/AI-色调 子组
· 标签按分类着色显示（蓝=场景/紫=风格/青=主体/橙=色调）

⚙️ 优化
· 标签去重清洗：去空格/括号/引号、全角转半角、过滤无意义词、同义词归并（匹配已有标签库）
· Prompt 调优：温度 0.7->0.3 更稳定、分类要求+示例、标签库 hint 扩到 100 个、只打标签时不生成文件名省 token
· 不覆盖已有标签分组：只对未分组的新标签归入 AI 子组（修复此前强制迁移已有标签的副作用）
