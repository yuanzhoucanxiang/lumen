✨ 新功能
· 库备份：启动时自动备份数据库（library.db.bak），设置页可手动备份 / 导出完整库 ZIP
· 视频卡片封面 = 四宫格故事板（悬停预览同样显示，短视频自动回退首帧）
· 视频播放支持：seek 拖动进度条，mov/mkv/avi/wmv 等格式悬停预览
· EXIF 元数据：导入时自动读取相机型号 / 拍摄时间 / 光圈 / 快门 / ISO / 焦距，检查器展示
· 文件夹视图联动子文件夹（看父文件夹时同时看到子孙文件夹的素材）
· 更新说明分类分点展示（✨ 新功能 / 🐛 修复 / ⚙️ 优化）
· 一键发布脚本：`node scripts/release.cjs` 自动打包发布（版本号、notes、Release、git tag）

🐛 修复
· 修复编辑器保存后无法恢复原图（原图与编辑版扩展名不同导致路径错乱，现原图永久保留可「恢复原图」）
· 修复视频导入失败（ffmpeg-static 无 ffprobe 改用 ffmpeg -i 探测时长；Windows Defender 锁定临时文件不再阻断导入）
· 修复视频无法播放（asset:// 协议补 MIME + Range 流式响应 + stream 特权）
· 修复视频卡片无封面显示（视频格式不在缩略图白名单内）
· 修复查重跨格式误判（按扩展名分组比较）

⚙️ 优化
· 导入提速：按 CPU 核心数并发处理，数据库写入原子事务（整批成功或整批回滚）
· 查询提速：构图 / 颜色数量筛选下推 SQL，新增 3 个索引
· 查重可调阈值 + 同格式分组，大库扫描更快
· 错误日志系统：所有失败可追溯（userData/logs/main.log），导入失败 toast 提示
· 编辑 / 恢复图片后自动刷新缓存（URL 版本戳）
